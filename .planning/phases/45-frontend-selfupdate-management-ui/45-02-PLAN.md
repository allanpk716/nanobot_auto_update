---
phase: 45-frontend-selfupdate-management-ui
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - internal/web/static/home.js
autonomous: false
requirements: [UI-03, UI-04, UI-05]

must_haves:
  truths:
    - "检测更新按钮获取最新版本号、发布日期、release notes 并展示"
    - "已有最新版本时显示已是最新版本提示"
    - "点击立即更新触发更新流程并显示进度"
    - "下载阶段实时显示进度百分比和进度条"
    - "更新完成后显示成功提示并自动刷新"
    - "更新失败时显示错误信息"
    - "更新进行中所有按钮被禁用"
  artifacts:
    - path: "internal/web/static/home.js"
      provides: "完整的更新交互逻辑（检测、触发、轮询、状态展示）"
      contains: "startProgressPolling"
  key_links:
    - from: "home.js checkUpdate"
      to: "GET /api/v1/self-update/check"
      via: "Bearer token fetch"
      pattern: "self-update/check.*Bearer"
    - from: "home.js startUpdate"
      to: "POST /api/v1/self-update"
      via: "POST fetch with Bearer token"
      pattern: "POST.*self-update"
    - from: "home.js startProgressPolling"
      to: "GET /api/v1/self-update/check"
      via: "500ms setInterval polling"
      pattern: "setInterval.*500"
---

<objective>
通过手动浏览器测试验证自更新管理 UI 的完整交互流程：检测更新、触发更新、进度轮询、状态反馈。

Purpose: Plan 01 已完成所有 HTML/CSS/JS 代码实现。本计划通过编译验证和浏览器测试确认完整功能。

Output: 验证通过的自更新管理 UI。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-frontend-selfupdate-management-ui/45-CONTEXT.md
@.planning/phases/45-frontend-selfupdate-management-ui/45-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编译并启动服务验证前端文件嵌入</name>
  <files>internal/web/static/home.html, internal/web/static/style.css, internal/web/static/home.js</files>
  <read_first>
    internal/web/static/home.html
    internal/web/static/style.css
    internal/web/static/home.js
  </read_first>
  <action>
验证 go:embed 正确嵌入修改后的静态文件：

1. 运行 `go build -o tmp/nanobot-test.exe ./cmd/nanobot-auto-updater/` 编译项目
2. 验证编译成功（exit code 0）
3. 用 `grep` 检查 home.html 中 selfupdate-section 的位置正确（在 `</header>` 之后、`<main>` 之前）
4. 用 `grep` 检查 home.js 中 initSelfUpdate 在 DOMContentLoaded 中被调用
5. 用 `grep` 检查 style.css 中包含 progress-bar-fill 样式

如果编译失败，修复问题（通常是由于 go:embed 路径不正确或文件编码问题）。
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/nanobot_auto_update && go build -o tmp/nanobot-test.exe ./cmd/nanobot-auto-updater/ && echo "BUILD OK" && grep -c "selfupdate-section" internal/web/static/home.html && grep -c "initSelfUpdate" internal/web/static/home.js && grep -c "progress-bar-fill" internal/web/static/style.css</automated>
  </verify>
  <done>
    - `go build` 编译成功，无错误
    - 静态文件嵌入正确，home.html 包含自更新区块
    - home.js 包含 initSelfUpdate 调用
    - style.css 包含进度条样式
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 浏览器验证自更新管理 UI 完整功能</name>
  <files>N/A (verification only)</files>
  <read_first>
    internal/web/static/home.html
    internal/web/static/style.css
    internal/web/static/home.js
  </read_first>
  <action>
Plan 01 完成了自更新管理 UI 的全部代码实现：
- home.html 新增自更新管理区块（header 和 main 之间）
- style.css 新增区块样式（版本标签、按钮、进度条、状态提示）
- home.js 新增完整 JS 逻辑（token 获取、版本显示、检测更新、触发更新、进度轮询）

Go 编译已验证通过，静态文件正确嵌入。本任务需要用户在浏览器中验证完整功能。
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/nanobot_auto_update && grep -c "selfupdate-section" internal/web/static/home.html && grep -c "startProgressPolling" internal/web/static/home.js</automated>
  </verify>
  <done>
    - 浏览器访问首页确认自更新管理区块显示在 header 和实例列表之间
    - 确认当前版本号以蓝色标签样式显示（如 v0.9.0）
    - 点击"检测更新"按钮正常工作：显示版本详情或"已是最新版本"
    - 点击"立即更新"触发更新：进度条 + 阶段文字 + 百分比显示正常
    - 更新成功显示绿色提示、失败显示红色错误信息，无 alert 弹窗
    - 用户回复 "approved"
  </done>
  <what-built>
Plan 01 完成了自更新管理 UI 的全部代码实现：
- home.html 新增自更新管理区块（header 和 main 之间）
- style.css 新增区块样式（版本标签、按钮、进度条、状态提示）
- home.js 新增完整 JS 逻辑（token 获取、版本显示、检测更新、触发更新、进度轮询）

Go 编译已验证通过，静态文件正确嵌入。
  </what-built>
  <how-to-verify>
请按以下步骤验证：

**前提：** 编译并启动服务（使用 `tmp/nanobot-test.exe` 或正常启动方式）

**验证步骤：**

1. **区块显示（UI-01）**
   - 浏览器访问 `http://localhost:<port>/`
   - 确认 header 和实例列表之间出现"自更新管理"区块
   - 确认区块始终展开，无折叠/展开按钮

2. **版本标签（UI-02）**
   - 确认"自更新管理"右侧显示当前版本号标签（蓝色底，如 `v0.9.0`）
   - 如果 API 不可用，显示 `--` 或 `N/A`

3. **检测更新（UI-03）**
   - 点击"检测更新"按钮
   - 按钮短暂显示"检测中..."后恢复
   - 如有新版本：确认显示最新版本号、发布日期（YYYY-MM-DD 格式）、release notes（截断显示+展开按钮）
   - 如已是最新：确认显示"已是最新版本"提示

4. **触发更新（UI-04）**
   - 如有新版本，"立即更新"按钮应变为可用
   - 点击"立即更新"
   - 确认按钮被禁用，"检测更新"按钮也被禁用
   - 确认进度状态显示（检查中 -> 下载中 XX% -> 安装中 -> 完成）

5. **下载进度（UI-05）**
   - 下载阶段确认蓝色进度条逐渐增长
   - 确认进度百分比文字实时更新
   - 进度条约 300px 宽

6. **完成/失败反馈**
   - 更新成功：绿色文字"更新完成，服务即将重启"
   - 更新失败：红色文字+错误信息
   - 确认没有 alert 弹窗

7. **非 localhost 测试（D-16）**
   - 如方便，从远程机器访问页面
   - 确认自更新区块显示"请在本地访问以使用自更新功能"
  </how-to-verify>
  <resume-signal>验证通过回复 "approved"，有问题请描述具体问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> localhost API | Self-update API calls from browser with Bearer token |
| browser -> remote API | Non-localhost access blocked at web-config level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05 | Elevation of Privilege | Self-update trigger from browser | mitigate | Bearer token required, localhost-only token distribution, button disabled during update |
</threat_model>

<verification>
1. `go build ./...` 编译成功
2. 浏览器验证所有 UI-01 到 UI-05 功能
3. 用户确认 approved
</verification>

<success_criteria>
- 用户在浏览器中确认自更新管理区块正确显示
- 检测更新功能正常（版本详情 + release notes）
- 触发更新功能正常（进度条 + 状态文字）
- 非 localhost 访问显示正确提示
- 用户回复 "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/45-frontend-selfupdate-management-ui/45-02-SUMMARY.md`
</output>
