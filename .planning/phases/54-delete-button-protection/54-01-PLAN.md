---
phase: 54-delete-button-protection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/web/static/home.js
  - internal/web/static/style.css
autonomous: true
requirements:
  - DEL-01

must_haves:
  truths:
    - "运行中实例的删除按钮显示为灰色且不可点击"
    - "停止实例的删除按钮正常可点击"
    - "5 秒轮询刷新后按钮状态与实例运行状态一致"
  artifacts:
    - path: "internal/web/static/home.js"
      provides: "deleteBtn.disabled = isRunning 禁用逻辑"
      contains: "deleteBtn.disabled = isRunning"
    - path: "internal/web/static/style.css"
      provides: ".btn-delete-danger:disabled 样式规则"
      contains: ".btn-delete-danger:disabled"
  key_links:
    - from: "internal/web/static/home.js"
      to: "internal/web/static/style.css"
      via: "JS 设置 disabled 属性，CSS :disabled 伪类控制视觉样式"
      pattern: "deleteBtn\\.disabled.*isRunning"
---

<objective>
运行中实例禁用删除按钮，防止误删服务

Purpose: 避免用户意外删除正在运行的实例导致服务中断。这是纯前端变更，利用已有的 `isRunning` 参数和项目现有的 `:disabled` CSS 模式。

Output: 修改后的 `home.js`（禁用逻辑）和 `style.css`（禁用样式）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-delete-button-protection/54-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 禁用运行中实例的删除按钮并添加禁用样式</name>
  <files>internal/web/static/home.js, internal/web/static/style.css</files>
  <read_first>
    internal/web/static/home.js (L1031-1035, deleteBtn 创建和事件绑定区域)
    internal/web/static/style.css (L526-528, .btn-action:disabled 现有禁用样式参考)
    internal/web/static/style.css (L823-826, .btn-form-danger:disabled 现有禁用样式参考)
    internal/web/static/style.css (L531-548, .btn-secondary 和 .btn-delete-danger 样式定义)
  </read_first>
  <action>
在 `internal/web/static/home.js` 的 `createInstanceCard()` 函数中，deleteBtn 创建后、addEventListener 之前（L1033 和 L1034 之间），添加一行：

```javascript
deleteBtn.disabled = isRunning;
```

这实现了 D-01（`deleteBtn.disabled = isRunning`）。`isRunning` 参数已经传入 `createInstanceCard(config, isRunning)`，无需额外获取。

在 `internal/web/static/style.css` 中，在 `.btn-delete-danger:hover` 规则（L545-548）之后添加新的 CSS 规则（D-02, D-03）：

```css
.btn-delete-danger:disabled {
    opacity: 0.6;
    cursor: not-allowed;
}
```

使用 `cursor: not-allowed` 而非 `cursor: wait`，因为这是"不可操作"语义（与 `.btn-form-danger:disabled` 一致），而非"操作进行中"语义（`.btn-action:disabled` 的 `cursor: wait`）。

不做任何其他改动。删除确认对话框（showDeleteDialog）保持不变（D-04）。
  </action>
  <acceptance_criteria>
    1. home.js L1033-L1035 区域包含 `deleteBtn.disabled = isRunning;`（在 `deleteBtn.className = ...` 之后、`deleteBtn.addEventListener('click', ...)` 之前）
    2. style.css 在 `.btn-delete-danger:hover` 规则之后包含 `.btn-delete-danger:disabled` 规则块
    3. `.btn-delete-danger:disabled` 规则包含 `opacity: 0.6;` 和 `cursor: not-allowed;`
    4. 没有对 showDeleteDialog 函数的任何修改
    5. 没有添加 tooltip、title 属性或文字变更
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\WorkSpace\agent\nanobot_auto_update" && grep -n "deleteBtn.disabled = isRunning" internal/web/static/home.js && grep -n "btn-delete-danger:disabled" internal/web/static/style.css && grep -A2 "btn-delete-danger:disabled" internal/web/static/style.css | grep "opacity: 0.6" && grep -A2 "btn-delete-danger:disabled" internal/web/static/style.css | grep "cursor: not-allowed"</automated>
  </verify>
  <done>
    - home.js 中 deleteBtn 创建后立即设置 `deleteBtn.disabled = isRunning`
    - style.css 中 `.btn-delete-danger:disabled` 规则使用 `opacity: 0.6; cursor: not-allowed;`
    - 删除确认对话框代码无变更
    - 运行中实例：删除按钮灰色不可点击；停止实例：删除按钮正常可用
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

无新增信任边界。此阶段为纯前端 UI 状态控制变更，不修改 API、后端逻辑或数据流。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | deleteBtn disabled | accept | 前端禁用仅为 UX 防误触，非安全控制。用户可通过浏览器 DevTools 绕过，但后端 DELETE API 已有认证保护（Bearer token），且删除前会自动停止实例（Phase 53 实现）。无安全风险。 |

安全影响评估：极低。此变更不引入新的攻击面，仅在前端层添加防误触保护。后端安全机制不受影响。
</threat_model>

<verification>
1. 打开 Web UI，查看运行中实例的卡片 — 删除按钮应为灰色（opacity 0.6），鼠标悬停显示 not-allowed 光标
2. 查看已停止实例的卡片 — 删除按钮正常显示，可点击，触发删除确认对话框
3. 启动一个实例，等待 5 秒状态轮询刷新 — 删除按钮变为灰色禁用
4. 停止该实例，等待 5 秒状态轮询刷新 — 删除按钮恢复可点击
</verification>

<success_criteria>
1. 运行中实例卡片上的删除按钮具有 disabled 属性，视觉呈灰色（opacity: 0.6），鼠标悬停显示 not-allowed 光标
2. 停止实例的删除按钮无 disabled 属性，可正常点击触发删除确认对话框
3. 5 秒轮询刷新后，按钮的 disabled 状态与 isRunning 值一致
4. 无 JavaScript 错误（浏览器控制台无报错）
</success_criteria>

<output>
After completion, create `.planning/phases/54-delete-button-protection/54-01-SUMMARY.md`
</output>
