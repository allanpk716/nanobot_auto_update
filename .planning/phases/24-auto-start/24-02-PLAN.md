---
phase: 24-auto-start
plan: 02
type: execute
wave: 2
depends_on:
  - 24-00
  - 24-01
files_modified:
  - internal/instance/lifecycle.go
  - internal/instance/manager.go
  - internal/instance/manager_test.go
autonomous: true
requirements:
  - AUTOSTART-02
  - AUTOSTART-03
  - AUTOSTART-04
must_haves:
  truths:
    - "用户启动应用后，所有配置的实例自动按顺序启动"
    - "用户可以通过日志看到每个实例的启动状态（成功或失败）"
    - "某个实例启动失败时，其他实例仍然继续启动"
    - "所有实例启动完成后，用户可以在日志中看到汇总状态"
  artifacts:
    - path: "internal/instance/manager.go"
      provides: "StartAllInstances 方法和 AutoStartResult 结构"
      exports: ["InstanceManager.StartAllInstances", "AutoStartResult"]
      contains: "StartAllInstances"
    - path: "internal/instance/lifecycle.go"
      provides: "InstanceLifecycle 辅助方法（Name, Port, ShouldAutoStart）"
      exports: ["InstanceLifecycle.Name", "InstanceLifecycle.Port", "InstanceLifecycle.ShouldAutoStart"]
    - path: "internal/instance/manager_test.go"
      provides: "StartAllInstances 单元测试（含辅助方法测试）"
      min_lines: 80
  key_links:
    - from: "InstanceManager.StartAllInstances"
      to: "InstanceLifecycle.StartAfterUpdate"
      via: "直接调用"
      pattern: "StartAfterUpdate"
    - from: "InstanceLifecycle.ShouldAutoStart"
      to: "InstanceConfig.ShouldAutoStart"
      via: "持有 config 字段引用"
      pattern: "il.config.ShouldAutoStart()"
    - from: "InstanceManager.StartAllInstances"
      to: "InstanceConfig.ShouldAutoStart"
      via: "通过 InstanceLifecycle 间接调用"
      pattern: "inst.ShouldAutoStart()"
---

<objective>
为 InstanceManager 添加 StartAllInstances 方法，实现串行启动所有 auto_start=true 的实例，支持优雅降级和汇总日志。
同时为 InstanceLifecycle 添加辅助方法以便访问配置字段。

Purpose: 实现应用启动时的自动化实例启动流程，无需用户手动干预。
Output: StartAllInstances 方法、AutoStartResult 结构体、InstanceLifecycle 辅助方法（Name, Port, ShouldAutoStart）。
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-auto-start/24-CONTEXT.md
@.planning/phases/24-auto-start/24-RESEARCH.md
@.planning/phases/24-auto-start/24-00-PLAN.md
@.planning/phases/24-auto-start/24-01-PLAN.md

<interfaces>
<!-- Key interfaces from existing codebase -->

From internal/instance/manager.go:
```go
type InstanceManager struct {
    instances []*InstanceLifecycle
    logger    *slog.Logger
}

func (m *InstanceManager) startAll(ctx context.Context, result *UpdateResult)
// 参考 startAll 的模式：串行执行、优雅降级、记录失败继续
```

From internal/instance/lifecycle.go:
```go
type InstanceLifecycle struct {
    config *config.InstanceConfig
    // ... other fields
}

func (il *InstanceLifecycle) StartAfterUpdate(ctx context.Context) error
// 返回 *InstanceError
```

From internal/instance/result.go:
```go
type UpdateResult struct {
    Stopped     []string         `json:"stopped"`
    Started     []string         `json:"started"`
    StopFailed  []*InstanceError `json:"stop_failed"`
    StartFailed []*InstanceError `json:"start_failed"`
}
```

From internal/config/instance.go (Phase 24-01 添加):
```go
func (ic *InstanceConfig) ShouldAutoStart() bool
// nil = 默认 true，显式设置才按设置值
```

From internal/instance/manager_test.go (Wave 0 created):
```go
func TestStartAllInstances(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-02")
}
func TestStartAllInstancesOrder(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-02")
}
func TestStartAllInstancesGracefulDegradation(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-02")
}
func TestStartAllInstancesSummary(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-02")
}
func TestInstanceLifecycleHelpers(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-02")
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add InstanceLifecycle helper methods</name>
  <files>
    - internal/instance/lifecycle.go
    - internal/instance/manager_test.go
  </files>
  <read_first>
    - internal/instance/lifecycle.go
    - internal/instance/manager_test.go
    - internal/config/instance.go (for InstanceConfig fields)
  </read_first>
  <behavior>
    - Test 1: Name() returns il.config.Name correctly
    - Test 2: Port() returns il.config.Port correctly
    - Test 3: ShouldAutoStart() delegates to il.config.ShouldAutoStart() correctly
  </behavior>
  <action>
1. 在 internal/instance/lifecycle.go 中添加 InstanceLifecycle 辅助方法：

```go
// Name 返回实例名称
func (il *InstanceLifecycle) Name() string {
    return il.config.Name
}

// Port 返回实例端口
func (il *InstanceLifecycle) Port() uint32 {
    return il.config.Port
}

// ShouldAutoStart 返回实例是否应该自动启动
// 委托到 InstanceConfig.ShouldAutoStart()
func (il *InstanceLifecycle) ShouldAutoStart() bool {
    return il.config.ShouldAutoStart()
}
```

2. 在 internal/instance/manager_test.go 更新 TestInstanceLifecycleHelpers 测试（移除 t.Skip，实现真实测试）：

```go
// TestInstanceLifecycleHelpers tests Name(), Port(), ShouldAutoStart() methods
func TestInstanceLifecycleHelpers(t *testing.T) {
    falseVal := false
    trueVal := true

    tests := []struct {
        name              string
        config            config.InstanceConfig
        wantName          string
        wantPort          uint32
        wantShouldAutoStart bool
    }{
        {
            name: "basic config with nil AutoStart",
            config: config.InstanceConfig{
                Name: "test-instance",
                Port: 18790,
                StartCommand: "echo test",
            },
            wantName:          "test-instance",
            wantPort:          18790,
            wantShouldAutoStart: true, // nil = default true
        },
        {
            name: "config with AutoStart=false",
            config: config.InstanceConfig{
                Name: "skip-instance",
                Port: 18791,
                StartCommand: "echo test",
                AutoStart: &falseVal,
            },
            wantName:          "skip-instance",
            wantPort:          18791,
            wantShouldAutoStart: false,
        },
        {
            name: "config with AutoStart=true",
            config: config.InstanceConfig{
                Name: "force-instance",
                Port: 18792,
                StartCommand: "echo test",
                AutoStart: &trueVal,
            },
            wantName:          "force-instance",
            wantPort:          18792,
            wantShouldAutoStart: true,
        },
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            // 创建最小化的 InstanceLifecycle 用于测试
            il := &InstanceLifecycle{
                config: &tt.config,
            }

            if got := il.Name(); got != tt.wantName {
                t.Errorf("Name() = %v, want %v", got, tt.wantName)
            }
            if got := il.Port(); got != tt.wantPort {
                t.Errorf("Port() = %v, want %v", got, tt.wantPort)
            }
            if got := il.ShouldAutoStart(); got != tt.wantShouldAutoStart {
                t.Errorf("ShouldAutoStart() = %v, want %v", got, tt.wantShouldAutoStart)
            }
        })
    }
}
```
</action>
  <verify>
    <automated>go test ./internal/instance -run TestInstanceLifecycleHelpers -v</automated>
  </verify>
  <done>
    - InstanceLifecycle 包含 Name(), Port(), ShouldAutoStart() 方法
    - TestInstanceLifecycleHelpers 测试通过
    - 所有三个辅助方法正确委托到 config 字段
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AutoStartResult struct and StartAllInstances method</name>
  <files>
    - internal/instance/manager.go
    - internal/instance/manager_test.go
  </files>
  <read_first>
    - internal/instance/manager.go
    - internal/instance/manager_test.go
    - internal/instance/result.go
    - internal/instance/lifecycle.go
    - internal/config/instance.go (for ShouldAutoStart method)
  </read_first>
  <behavior>
    - Test 1 (AUTOSTART-02): StartAllInstances 启动所有 auto_start=true 的实例
    - Test 2 (AUTOSTART-02): StartAllInstances 跳过 auto_start=false 的实例
    - Test 3 (AUTOSTART-02): StartAllInstances 按配置顺序依次启动（串行）
    - Test 4 (AUTOSTART-03): StartAllInstances 失败时继续启动其他实例（优雅降级）- 独立验证
    - Test 5 (AUTOSTART-04): StartAllInstances 返回包含成功/失败/跳过的 AutoStartResult - 独立验证
    - Test 6 (AUTOSTART-04): 日志汇总包含成功/失败/跳过数量和失败实例名称
  </behavior>
  <action>
1. 在 internal/instance/manager.go 中添加 AutoStartResult 结构体：

```go
// AutoStartResult 包含自动启动流程的结果
type AutoStartResult struct {
    Started []string         `json:"started"`     // 成功启动的实例名称
    Failed  []*InstanceError `json:"failed"`      // 启动失败的实例错误
    Skipped []string         `json:"skipped"`     // 跳过自动启动的实例（auto_start: false）
}
```

2. 在 InstanceManager 添加 StartAllInstances 方法（使用中文日志）：

```go
// StartAllInstances 启动所有配置为自动启动的实例（串行执行，优雅降级）
// AUTOSTART-02, AUTOSTART-03, AUTOSTART-04
func (m *InstanceManager) StartAllInstances(ctx context.Context) *AutoStartResult {
    m.logger.Info("开始自动启动阶段", "instance_count", len(m.instances))

    result := &AutoStartResult{}
    startTime := time.Now()

    for _, inst := range m.instances {
        // 通过 InstanceLifecycle 访问 InstanceConfig.ShouldAutoStart()
        if !inst.ShouldAutoStart() {
            m.logger.Info("跳过实例（auto_start=false）",
                "instance", inst.Name(),
                "port", inst.Port())
            result.Skipped = append(result.Skipped, inst.Name())
            continue
        }

        // 记录单个实例启动时间
        instStart := time.Now()
        m.logger.Info("正在启动实例",
            "instance", inst.Name(),
            "port", inst.Port())

        if err := inst.StartAfterUpdate(ctx); err != nil {
            duration := time.Since(instStart)
            m.logger.Error("启动实例失败",
                "error", err,
                "instance", inst.Name(),
                "port", inst.Port(),
                "duration", duration)
            // 记录失败但继续启动其他实例（优雅降级）
            result.Failed = append(result.Failed, err.(*InstanceError))
        } else {
            duration := time.Since(instStart)
            m.logger.Info("实例启动成功",
                "instance", inst.Name(),
                "port", inst.Port(),
                "duration", duration)
            result.Started = append(result.Started, inst.Name())
        }
    }

    // 记录汇总日志
    totalDuration := time.Since(startTime)
    failedNames := extractNames(result.Failed)

    if len(result.Failed) > 0 {
        m.logger.Warn("自动启动完成（部分失败）",
            "started", len(result.Started),
            "failed", len(result.Failed),
            "skipped", len(result.Skipped),
            "failed_instances", failedNames,
            "total_duration", totalDuration)
    } else {
        m.logger.Info("自动启动完成",
            "started", len(result.Started),
            "failed", len(result.Failed),
            "skipped", len(result.Skipped),
            "total_duration", totalDuration)
    }

    return result
}
```

3. 添加 extractNames 辅助函数：

```go
// extractNames 从 InstanceError 切片中提取实例名称
func extractNames(errors []*InstanceError) []string {
    names := make([]string, len(errors))
    for i, err := range errors {
        names[i] = err.InstanceName
    }
    return names
}
```

4. 在 internal/instance/manager_test.go 更新测试（移除 t.Skip，实现真实测试）：

```go
// TestStartAllInstances_AutoStartFlag tests AUTOSTART-02:
// 启动所有 auto_start=true 的实例，跳过 auto_start=false 的实例
func TestStartAllInstances_AutoStartFlag(t *testing.T) {
    logger := slog.New(slog.NewTextHandler(os.Stdout, nil))

    tests := []struct {
        name           string
        instances      []config.InstanceConfig
        wantStarted    int
        wantSkipped    int
        wantFailed     int
    }{
        {
            name: "all instances auto_start=nil (default true)",
            instances: []config.InstanceConfig{
                {Name: "inst1", Port: 8090, StartCommand: "nonexistent"},
                {Name: "inst2", Port: 8091, StartCommand: "nonexistent"},
            },
            wantStarted: 0, // 命令不存在，全部失败
            wantSkipped: 0,
            wantFailed:  2,
        },
        {
            name: "one instance skipped",
            instances: []config.InstanceConfig{
                {Name: "inst1", Port: 8090, StartCommand: "nonexistent"},
                {Name: "inst2", Port: 8091, StartCommand: "nonexistent", AutoStart: ptrBool(false)},
            },
            wantStarted: 0,
            wantSkipped: 1,
            wantFailed:  1,
        },
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            cfg := &config.Config{Instances: tt.instances}
            manager := NewInstanceManager(cfg, logger)

            ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)
            defer cancel()

            result := manager.StartAllInstances(ctx)

            if len(result.Started) != tt.wantStarted {
                t.Errorf("Started = %d, want %d", len(result.Started), tt.wantStarted)
            }
            if len(result.Skipped) != tt.wantSkipped {
                t.Errorf("Skipped = %d, want %d", len(result.Skipped), tt.wantSkipped)
            }
            if len(result.Failed) != tt.wantFailed {
                t.Errorf("Failed = %d, want %d", len(result.Failed), tt.wantFailed)
            }
        })
    }
}

// TestStartAllInstances_Order tests AUTOSTART-02:
// 实例按配置顺序依次启动（串行）
func TestStartAllInstances_Order(t *testing.T) {
    // 验证实例按配置顺序依次启动
    // 可以通过检查日志输出或使用 mock 记录调用顺序
}

// TestStartAllInstances_GracefulDegradation tests AUTOSTART-03 (独立验证):
// 失败时继续启动其他实例（优雅降级）
func TestStartAllInstances_GracefulDegradation(t *testing.T) {
    logger := slog.New(slog.NewTextHandler(os.Stdout, nil))

    // 创建两个实例：第一个会失败，第二个也应该尝试启动
    instances := []config.InstanceConfig{
        {Name: "failing-inst", Port: 8090, StartCommand: "nonexistent_command"},
        {Name: "also-failing", Port: 8091, StartCommand: "another_nonexistent"},
    }

    cfg := &config.Config{Instances: instances}
    manager := NewInstanceManager(cfg, logger)

    ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
    defer cancel()

    result := manager.StartAllInstances(ctx)

    // 关键验证：两个实例都应该被尝试（即使第一个失败）
    // 由于命令不存在，两个都会失败，但都应该在 Failed 列表中
    if len(result.Failed) != 2 {
        t.Errorf("Failed count = %d, want 2 (both instances should be attempted)", len(result.Failed))
    }
    if len(result.Started) != 0 {
        t.Errorf("Started count = %d, want 0", len(result.Started))
    }
}

// TestStartAllInstances_Summary tests AUTOSTART-04 (独立验证):
// 返回包含成功/失败/跳过的 AutoStartResult，日志汇总包含正确信息
func TestStartAllInstances_Summary(t *testing.T) {
    logger := slog.New(slog.NewTextHandler(os.Stdout, nil))

    falseVal := false
    instances := []config.InstanceConfig{
        {Name: "skip-me", Port: 8090, StartCommand: "echo skip", AutoStart: &falseVal},
        {Name: "fail-me", Port: 8091, StartCommand: "nonexistent"},
    }

    cfg := &config.Config{Instances: instances}
    manager := NewInstanceManager(cfg, logger)

    ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)
    defer cancel()

    result := manager.StartAllInstances(ctx)

    // 验证 AutoStartResult 结构正确填充
    if len(result.Skipped) != 1 || result.Skipped[0] != "skip-me" {
        t.Errorf("Skipped = %v, want [skip-me]", result.Skipped)
    }
    if len(result.Failed) != 1 {
        t.Errorf("Failed count = %d, want 1", len(result.Failed))
    }
    if len(result.Started) != 0 {
        t.Errorf("Started count = %d, want 0", len(result.Started))
    }

    // 验证 Failed 中的 InstanceError 包含正确的实例名称
    if len(result.Failed) > 0 && result.Failed[0].InstanceName != "fail-me" {
        t.Errorf("Failed[0].InstanceName = %v, want fail-me", result.Failed[0].InstanceName)
    }
}

func ptrBool(v bool) *bool { return &v }
```

5. 添加必要的 import：
   - "time" 用于时间统计
</action>
  <verify>
    <automated>go test ./internal/instance -run "TestStartAllInstances|TestInstanceLifecycleHelpers" -v</automated>
  </verify>
  <done>
    - AutoStartResult 结构体包含 Started, Failed, Skipped 字段
    - StartAllInstances 方法存在且正常工作
    - Name(), Port(), ShouldAutoStart() 辅助方法存在
    - 串行启动：按配置顺序
    - 优雅降级：失败不中断（TestStartAllInstances_GracefulDegradation 独立验证 AUTOSTART-03）
    - 汇总结果：AutoStartResult 包含正确字段（TestStartAllInstances_Summary 独立验证 AUTOSTART-04）
    - 所有测试通过：`go test ./internal/instance -run "TestStartAllInstances|TestInstanceLifecycleHelpers" -v`
  </done>
</task>

</tasks>

<verification>
功能验证：
- [ ] AutoStartResult 结构体存在
- [ ] StartAllInstances 方法存在
- [ ] Name(), Port(), ShouldAutoStart() 方法存在
- [ ] auto_start=false 的实例被跳过
- [ ] 失败实例不中断流程（AUTOSTART-03 独立测试）
- [ ] AutoStartResult 汇总正确（AUTOSTART-04 独立测试）
- [ ] 日志汇总包含所有必要信息
</verification>

<success_criteria>
- StartAllInstances 方法实现完整
- 优雅降级行为正确（失败继续）- AUTOSTART-03 独立测试验证
- AutoStartResult 汇总结构正确 - AUTOSTART-04 独立测试验证
- 辅助方法测试覆盖完整
- go test ./internal/instance -run "TestStartAllInstances|TestInstanceLifecycleHelpers" 通过
</success_criteria>

<output>
After completion, create `.planning/phases/24-auto-start/24-02-SUMMARY.md`
</output>
