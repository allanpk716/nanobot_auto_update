---
phase: 24-auto-start
plan: 03
type: execute
wave: 3
depends_on:
  - 24-02
files_modified:
  - cmd/nanobot-auto-updater/main.go
autonomous: true
requirements:
  - AUTOSTART-01
must_haves:
  truths:
    - "应用启动后，API 服务器先准备好，然后实例自动启动"
    - "实例启动不阻塞主流程（在 goroutine 中异步执行）"
    - "goroutine panic 不会导致应用崩溃（有 recover 保护）"
  artifacts:
    - path: "cmd/nanobot-auto-updater/main.go"
      provides: "应用启动入口和自动启动触发"
      contains: "StartAllInstances"
  key_links:
    - from: "main.go"
      to: "InstanceManager.StartAllInstances"
      via: "goroutine 异步调用"
      pattern: "go func.*StartAllInstances"
---

<objective>
在 main.go 中集成自动启动逻辑，在 API 服务器启动后异步启动所有实例。

Purpose: 确保应用启动时自动启动所有配置的实例，无需用户手动干预。
Output: main.go 包含异步启动实例的 goroutine 调用。
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-auto-start/24-CONTEXT.md
@.planning/phases/24-auto-start/24-RESEARCH.md
@.planning/phases/24-auto-start/24-02-PLAN.md

<interfaces>
<!-- Key interfaces from existing codebase -->

From cmd/nanobot-auto-updater/main.go (current):
```go
// Start API server in goroutine
go func() {
    logger.Info("Starting API server", "port", cfg.API.Port)
    if err := apiServer.Start(); err != nil {
        logger.Error("API server error", "error", err)
    }
}()

// Setup graceful shutdown signal handling
sigChan := make(chan os.Signal, 1)
signal.Notify(sigChan, syscall.SIGINT, syscall.SIGTERM)

// Wait for shutdown signal
<-sigChan
```

From internal/instance/manager.go (Phase 24-02):
```go
func (m *InstanceManager) StartAllInstances(ctx context.Context) *AutoStartResult
```

From CONTEXT.md locked decision:
- 启动顺序：配置加载 → InstanceManager 创建 → API 服务器启动 → 自动启动实例
- 实例启动在 goroutine 中执行，不阻塞 API 服务器启动
- 实现：在 main.go 中，API 服务器启动 goroutine 之后启动实例
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate auto-start in main.go</name>
  <files>
    - cmd/nanobot-auto-updater/main.go
  </files>
  <read_first>
    - cmd/nanobot-auto-updater/main.go
    - internal/instance/manager.go (for StartAllInstances signature)
  </read_first>
  <action>
在 cmd/nanobot-auto-updater/main.go 中添加自动启动逻辑：

1. 在 API 服务器启动 goroutine 之后，添加自动启动实例的 goroutine：

```go
// Start API server in goroutine
go func() {
    logger.Info("Starting API server", "port", cfg.API.Port)
    if err := apiServer.Start(); err != nil {
        logger.Error("API server error", "error", err)
    }
}()

// Auto-start instances in goroutine (non-blocking)
// AUTOSTART-01: Application starts all configured instances at startup
go func() {
    defer func() {
        if r := recover(); r != nil {
            logger.Error("自动启动 goroutine panic",
                "panic", r,
                "stack", string(debug.Stack()))
        }
    }()

    // Create context with timeout for auto-start
    // Total timeout: 5 minutes (adjust based on instance count)
    autoStartTimeout := 5 * time.Minute
    autoStartCtx, cancel := context.WithTimeout(context.Background(), autoStartTimeout)
    defer cancel()

    logger.Info("开始自动启动所有实例",
        "instance_count", len(cfg.Instances),
        "timeout", autoStartTimeout)

    // Execute auto-start
    instanceManager.StartAllInstances(autoStartCtx)
}()
```

2. 添加必要的 import：
```go
import (
    // ... existing imports ...
    "runtime/debug"  // for debug.Stack()
)
```

3. 注意事项：
   - autoStartTimeout 设置为 5 分钟，足够多个实例启动
   - 使用 defer recover() 捕获 panic，防止应用崩溃
   - 不阻塞主流程，API 服务器可以立即响应请求
   - 日志记录启动开始和完成

4. 实现位置：在 API 服务器启动 goroutine 和信号处理之间
</action>
  <verify>
    <automated>grep -n "StartAllInstances" cmd/nanobot-auto-updater/main.go</automated>
  </verify>
  <done>
    - main.go 包含 go func() { instanceManager.StartAllInstances(...) }() 调用
    - 有 defer recover() 保护
    - 使用 context.WithTimeout 设置超时
    - 有启动日志输出
    - go build ./cmd/nanobot-auto-updater 成功
  </done>
</task>

</tasks>

<verification>
集成验证：
- [ ] main.go 包含 StartAllInstances 调用
- [ ] 调用在 goroutine 中（不阻塞）
- [ ] 有 panic recover 保护
- [ ] 有 context 超时控制
- [ ] go build 成功
</verification>

<success_criteria>
- go build ./cmd/nanobot-auto-updater 成功
- main.go 包含自动启动逻辑
- 异步执行，不阻塞 API 服务器
- panic 保护存在
</success_criteria>

<output>
After completion, create `.planning/phases/24-auto-start/24-03-SUMMARY.md`
</output>
