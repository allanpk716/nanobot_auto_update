---
phase: 24-auto-start
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/config/instance_test.go
  - internal/instance/manager_test.go
autonomous: true
requirements:
  - AUTOSTART-01
  - AUTOSTART-02
  - AUTOSTART-03
  - AUTOSTART-04
must_haves:
  truths:
    - "测试框架已准备好，支持后续实现计划的验证"
    - "所有必需的测试桩已创建，覆盖所有需求（AUTOSTART-01, 02, 03, 04）"
  artifacts:
    - path: "internal/config/instance_test.go"
      provides: "TestInstanceConfigAutoStart 测试桩"
      contains: "TestInstanceConfigAutoStart"
    - path: "internal/instance/manager_test.go"
      provides: "TestStartAllInstances* 测试桩（含辅助方法测试）"
      contains: "TestStartAllInstances"
  key_links:
    - from: "测试文件"
      to: "实现代码（后续）"
      via: "TDD 流程"
      pattern: "t.Skip"
---

<objective>
为 AutoStart 功能创建测试桩文件，确保后续计划有测试框架可用。

Purpose: 遵循 Nyquist 采样原则，先创建测试框架，后续计划填充实现。Wave 0 在所有实现计划之前执行。
Output: 两个测试文件中的测试桩函数（标记为 t.Skip）。
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-auto-start/24-CONTEXT.md
@.planning/phases/24-auto-start/24-VALIDATION.md

<interfaces>
<!-- 现有测试文件结构 -->

From internal/config/instance_test.go:
```go
// 已有测试: TestInstanceConfigValidation
// 需要添加: TestInstanceConfigAutoStart
```

From internal/instance/manager_test.go:
```go
// 已有测试: TestNewInstanceManager, TestStopAllGracefulDegradation, TestStartAllGracefulDegradation
// 需要添加: TestStartAllInstances, TestStartAllInstancesOrder, TestStartAllInstancesGracefulDegradation, TestStartAllInstancesSummary, TestInstanceLifecycleHelpers
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TestInstanceConfigAutoStart test stub</name>
  <files>
    - internal/config/instance_test.go
  </files>
  <read_first>
    - internal/config/instance_test.go
  </read_first>
  <action>
在 internal/config/instance_test.go 文件末尾添加测试桩：

```go
// TestInstanceConfigAutoStart tests AUTOSTART-01:
// InstanceConfig.AutoStart field and ShouldAutoStart() method
// Wave 0: Test stub - implementation in Plan 24-01
func TestInstanceConfigAutoStart(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-01")

    // Test cases (to be implemented in Plan 24-01):
    // 1. nil AutoStart defaults to true (ShouldAutoStart returns true)
    // 2. explicit *false has ShouldAutoStart return false
    // 3. explicit *true has ShouldAutoStart return true
}
```

注意：
- 使用 t.Skip() 标记测试为待实现状态
- 添加注释说明这是 Wave 0 测试桩
- 包含测试用例描述，供 Plan 24-01 参考
</action>
  <verify>
    <automated>grep -n "TestInstanceConfigAutoStart" internal/config/instance_test.go</automated>
  </verify>
  <done>
    - internal/config/instance_test.go 包含 TestInstanceConfigAutoStart 函数
    - 函数包含 t.Skip("MISSING - Implementation in Plan 24-01")
    - 测试文件可以编译（go build ./internal/config）
  </done>
</task>

<task type="auto">
  <name>Task 2: Create StartAllInstances test stubs and helper method tests</name>
  <files>
    - internal/instance/manager_test.go
  </files>
  <read_first>
    - internal/instance/manager_test.go
  </read_first>
  <action>
在 internal/instance/manager_test.go 文件末尾添加测试桩：

```go
// TestStartAllInstances tests AUTOSTART-02:
// InstanceManager.StartAllInstances method behavior
// Wave 0: Test stub - implementation in Plan 24-02
func TestStartAllInstances(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-02")

    // Test cases (to be implemented in Plan 24-02):
    // 1. StartAllInstances starts all auto_start=true instances
    // 2. StartAllInstances skips auto_start=false instances
    // 3. StartAllInstances returns AutoStartResult with correct counts
}

// TestStartAllInstancesOrder tests AUTOSTART-02:
// Instances are started in configuration order (serial)
// Wave 0: Test stub - implementation in Plan 24-02
func TestStartAllInstancesOrder(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-02")

    // Test case (to be implemented in Plan 24-02):
    // Verify instances are started in the order they appear in config
}

// TestStartAllInstancesGracefulDegradation tests AUTOSTART-03:
// Failed instance does not prevent other instances from starting
// Wave 0: Test stub - implementation in Plan 24-02
func TestStartAllInstancesGracefulDegradation(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-02")

    // Test case (to be implemented in Plan 24-02):
    // First instance fails, second instance still starts
}

// TestStartAllInstancesSummary tests AUTOSTART-04:
// AutoStartResult contains summary with started/failed/skipped counts
// Wave 0: Test stub - implementation in Plan 24-02
func TestStartAllInstancesSummary(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-02")

    // Test cases (to be implemented in Plan 24-02):
    // 1. Result.Started contains successfully started instance names
    // 2. Result.Failed contains InstanceError for failed instances
    // 3. Result.Skipped contains skipped instance names (auto_start=false)
}

// TestInstanceLifecycleHelpers tests AUTOSTART-01 (indirect):
// Name(), Port(), ShouldAutoStart() helper methods on InstanceLifecycle
// Wave 0: Test stub - implementation in Plan 24-02
func TestInstanceLifecycleHelpers(t *testing.T) {
    t.Skip("MISSING - Implementation in Plan 24-02")

    // Test cases (to be implemented in Plan 24-02):
    // 1. Name() returns il.config.Name correctly
    // 2. Port() returns il.config.Port correctly
    // 3. ShouldAutoStart() delegates to il.config.ShouldAutoStart() correctly
}
```

注意：
- 每个测试函数对应一个具体的行为验证
- AUTOSTART-03 (GracefulDegradation) 和 AUTOSTART-04 (Summary) 有独立的测试函数
- TestInstanceLifecycleHelpers 覆盖辅助方法的测试
- 使用 t.Skip() 标记为待实现状态
- 包含测试用例描述，供 Plan 24-02 参考
</action>
  <verify>
    <automated>grep -n "TestStartAllInstances\|TestInstanceLifecycleHelpers" internal/instance/manager_test.go | head -15</automated>
  </verify>
  <done>
    - internal/instance/manager_test.go 包含 5 个测试函数（4 个 TestStartAllInstances* + 1 个 TestInstanceLifecycleHelpers）
    - 每个函数包含 t.Skip("MISSING - Implementation in Plan 24-02")
    - 测试文件可以编译（go build ./internal/instance）
  </done>
</task>

</tasks>

<verification>
Wave 0 完成标准：
- [ ] internal/config/instance_test.go 包含 TestInstanceConfigAutoStart
- [ ] internal/instance/manager_test.go 包含 TestStartAllInstances*
- [ ] internal/instance/manager_test.go 包含 TestInstanceLifecycleHelpers（覆盖辅助方法）
- [ ] 所有测试桩使用 t.Skip() 标记
- [ ] go build ./internal/config 和 go build ./internal/instance 成功
</verification>

<success_criteria>
- 测试桩文件创建完成
- 后续计划 (24-01, 24-02) 可以基于测试桩实现真实测试
- VALIDATION.md wave_0_complete 可更新为 true
</success_criteria>

<output>
After completion, create `.planning/phases/24-auto-start/24-00-SUMMARY.md`
</output>
