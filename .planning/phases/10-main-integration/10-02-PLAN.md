---
phase: 10-main-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [cmd/nanobot-auto-updater/main.go]
autonomous: false
requirements: []
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "多实例配置加载后，日志显示每个实例的详细信息（名称、端口、启动命令）"
  artifacts:
    - path: "cmd/nanobot-auto-updater/main.go"
      provides: "多实例配置详细信息日志输出"
      min_lines: 5
      contains: "Instance configuration"
  key_links:
    - from: "cmd/nanobot-auto-updater/main.go"
      to: "cfg.Instances"
      via: "for循环遍历"
      pattern: "for.*range.*cfg.Instances"
---

<objective>
增强多实例配置加载后的日志输出,为每个实例显示详细信息（名称、端口、启动命令）,使用户能够清晰地了解加载了哪些实例配置。

Purpose: 提升用户体验,通过日志让用户直观地看到多实例配置已正确加载,包括每个实例的具体参数。
Output: main.go 中多实例模式检测代码增强,包含实例详细信息循环输出。
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Gap Context

**Source:** 10-UAT.md (Test 2: 多实例配置加载)
**Issue:** 用户反馈启动时看不到实例的名称、端口配置和启动命令
**Root Cause:** main.go 第 141-145 行只输出实例总数,缺少实例详细信息遍历输出
**Expected Behavior:**
```
2026-03-12 08:25:00.124 - [INFO]: Running in multi-instance mode instance_count=2
2026-03-12 08:25:00.125 - [INFO]: Instance configuration instance_number=1 name=gateway port=18790 start_command=echo test-gateway
2026-03-12 08:25:00.126 - [INFO]: Instance configuration instance_number=2 name=worker port=18791 start_command=echo test-worker
```

## Existing Implementation

From cmd/nanobot-auto-updater/main.go (line 141-145):
```go
if useMultiInstance {
    logger.Info("Running in multi-instance mode", "instance_count", len(cfg.Instances))
} else {
    logger.Info("Running in legacy single-instance mode", "port", cfg.Nanobot.Port)
}
```

## Suggested Fix

在多实例模式检测后添加 for 循环遍历 cfg.Instances,为每个实例输出:
- instance_number: 实例序号 (1-based)
- name: 实例名称
- port: 实例端口
- start_command: 启动命令

参考 Phase 07-01 的日志模式:使用 logger.With() 预注入 instance 和 component 字段。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强多实例配置日志输出</name>
  <files>cmd/nanobot-auto-updater/main.go</files>
  <action>
在 main.go 第 142 行后添加实例详细信息日志循环:

1. 保持现有的 "Running in multi-instance mode" 日志输出实例总数
2. 在其后添加 for 循环遍历 cfg.Instances:
   ```go
   // 输出每个实例的配置信息
   for i, inst := range cfg.Instances {
       logger.Info("Instance configuration",
           "instance_number", i+1,
           "name", inst.Name,
           "port", inst.Port,
           "start_command", inst.StartCommand)
   }
   ```
3. 确保日志格式与项目规范一致 (使用 slog 的键值对格式)
4. 不修改 legacy 单实例模式的日志输出

**注意:** 实例序号使用 1-based (i+1) 以符合用户直觉。
**避免:** 不要在日志中使用中文,保持代码整洁。
  </action>
  <verify>
    <automated>go build -o nanobot-auto-updater.exe ./cmd/nanobot-auto-updater</automated>
  </verify>
  <done>
    - 编译成功无错误
    - 多实例模式启动时日志输出实例总数
    - 每个实例的日志包含: instance_number, name, port, start_command
    - Legacy 模式不受影响
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 手动验证多实例配置日志输出</name>
  <files>N/A</files>
  <action>等待用户验证日志输出是否符合预期</action>
  <what-built>增强的多实例配置日志输出功能</what-built>
  <how-to-verify>
### 手动验证步骤:

1. **编译程序:**
   ```bash
   go build -o nanobot-auto-updater.exe ./cmd/nanobot-auto-updater
   ```

2. **使用多实例配置启动程序:**
   ```bash
   ./nanobot-auto-updater.exe --config tmp/test_multi_instance.yaml --update-now --timeout 30s
   ```

3. **检查日志输出:**
   在 `logs/` 目录下的最新日志文件中查找:
   - ✓ 应包含 "Running in multi-instance mode instance_count=2"
   - ✓ 应包含 "Instance configuration instance_number=1 name=gateway port=18790 start_command=echo test-gateway"
   - ✓ 应包含 "Instance configuration instance_number=2 name=worker port=18791 start_command=echo test-worker"

4. **验证 Legacy 模式不受影响:**
   ```bash
   ./nanobot-auto-updater.exe --config tmp/test_legacy.yaml --update-now --timeout 30s
   ```
   检查日志包含 "Running in legacy single-instance mode port=18790"
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
  <verify>用户确认所有检查项通过</verify>
  <done>用户批准日志输出符合预期</done>
</task>

</tasks>

<verification>
### 自动验证
- [x] 编译成功无错误
- [x] 代码格式符合项目规范 (gofmt)
- [x] 无 go vet 警告

### 手动验证
- [ ] 多实例配置启动时日志输出实例总数
- [ ] 每个实例的日志包含所有必需字段 (name, port, start_command)
- [ ] 实例序号正确 (1-based)
- [ ] Legacy 模式日志不受影响
</verification>

<success_criteria>
**必须为真的条件:**
1. 用户启动多实例配置时能看到每个实例的名称
2. 用户能从日志中了解每个实例的端口配置
3. 用户能从日志中了解每个实例的启动命令
4. 实例总数和详细信息都清晰可见
5. Legacy 模式保持向后兼容
</success_criteria>

<output>
After completion, create `.planning/phases/10-main-integration/10-02-SUMMARY.md`
</output>
