---
phase: 42-telegram-monitor-core
plan: 02
type: tdd
wave: 2
depends_on: ["42-01"]
files_modified:
  - internal/telegram/monitor_test.go
  - internal/telegram/monitor.go
autonomous: true
requirements: [TELE-01, TELE-02, TELE-03, TELE-04, TELE-05, TELE-06, TELE-08]

must_haves:
  truths:
    - "No data races between processEntry goroutine and AfterFunc timer callback"
    - "Monitor handles rapid trigger-success-trigger-failure sequences correctly"
    - "Notification goroutine panic does not crash the monitor"
    - "Full test suite passes with -race flag enabled"
  artifacts:
    - path: "internal/telegram/monitor_test.go"
      provides: "Edge case and concurrency tests added to existing test file"
      min_lines: 200
  key_links:
    - from: "internal/telegram/monitor_test.go"
      to: "internal/telegram/monitor.go"
      via: "Race detector validates mutex-protected state access"
      pattern: "go test.*-race"
---

<objective>
Validate the Telegram monitor under concurrent stress and edge cases. Add race detector tests, panic recovery tests, and rapid state transition tests to ensure production robustness.

Purpose: The AfterFunc timer callback runs in a separate goroutine from the subscriber read loop. Race conditions and panic propagation are the most likely failure modes in production. This plan hardens the monitor against those failure modes.

Output: Additional test cases in monitor_test.go covering concurrency, panic recovery, and edge cases. All tests pass with -race flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-telegram-monitor-core/42-01-SUMMARY.md
@.planning/phases/42-telegram-monitor-core/42-RESEARCH.md

<interfaces>
<!-- Types from Plan 01 that this plan's tests exercise -->

From internal/telegram/monitor.go (created by Plan 01):
```go
type TelegramMonitor struct { ... }
func NewTelegramMonitor(logBuffer LogSubscriber, notifier Notifier, instanceName string, timeout time.Duration, logger *slog.Logger) *TelegramMonitor
func (m *TelegramMonitor) Start(ctx context.Context)
func (m *TelegramMonitor) Stop()

type LogSubscriber interface {
    Subscribe() <-chan logbuffer.LogEntry
    Unsubscribe(ch <-chan logbuffer.LogEntry)
}

type Notifier interface {
    IsEnabled() bool
    Notify(title, message string) error
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write edge case and concurrency stress tests</name>
  <files>internal/telegram/monitor_test.go</files>

  <read_first>
    - internal/telegram/monitor.go (current implementation — verify state transitions, mutex usage, goroutine patterns)
    - internal/telegram/monitor_test.go (existing tests — append new tests)
    - internal/telegram/patterns.go (pattern constants used in test assertions)
  </read_first>

  <behavior>
  Add these test functions to the existing monitor_test.go:

  1. TestMonitor_RapidTriggerSuccessSequence: Send trigger+success+trigger+success rapidly (no sleep between entries). Verify 2 success notifications received, no failure/timeout notifications.

  2. TestMonitor_RapidTriggerFailureSequence: Send trigger+failure+trigger+failure rapidly. Verify 2 failure notifications, no success/timeout.

  3. TestMonitor_TriggerFollowedByAnotherTrigger: Send two trigger entries without success/failure between them. Verify only ONE timeout notification fires (timer restarted, not duplicated).

  4. TestMonitor_NotificationPanicRecovery: Create mockNotifier where Notify() panics. Send trigger+failure. Verify monitor goroutine survives (does not crash), no deadlock.

  5. TestMonitor_DisabledNotifierSucceeds: Create mockNotifier with enabled=false. Send trigger+success. Verify no panic, no error, monitor continues running.

  6. TestMonitor_ContextCancelledBeforeTrigger: Cancel context before any log entry. Verify no notification, no panic, no goroutine leak (monitor exits cleanly).

  7. TestMonitor_EmptyContentEntry: Send LogEntry with empty Content. Verify no state change, no notification.

  8. TestMonitor_ConcurrentTimerAndProcessEntry: Send trigger entry, then immediately send success entry in rapid succession with very short timeout (50ms). This tests the race between timer firing and processEntry resolving. Run with -race to validate.
  </behavior>

  <action>
Open the existing internal/telegram/monitor_test.go file. APPEND (do not modify existing tests) the 8 new test functions listed in the behavior block above.

For TestMonitor_NotificationPanicRecovery:
```go
type panicNotifier struct {
    mu      sync.Mutex
    panicked bool
}
func (p *panicNotifier) IsEnabled() bool { return true }
func (p *panicNotifier) Notify(title, message string) error {
    p.mu.Lock()
    p.panicked = true
    p.mu.Unlock()
    panic("intentional test panic")
}
```
After sending trigger+failure, sleep 100ms, then verify the test has not deadlocked (reaches assertion). The monitor's defer-recover in sendNotification should catch the panic.

For TestMonitor_ConcurrentTimerAndProcessEntry:
Use 50ms timeout. Send trigger, then immediately send success. Assert either success notification or timeout notification appears (not both). This is a race-positive test — both outcomes are valid since the timing is non-deterministic, but exactly ONE notification should appear.

For TestMonitor_TriggerFollowedByAnotherTrigger:
Send trigger, sleep 50ms (timeout is 200ms), send another trigger. Then wait for timeout. Verify exactly 1 timeout notification (timer was restarted on second trigger).

For TestMonitor_RapidTriggerSuccessSequence:
Send all 4 entries (trigger, success, trigger, success) with zero sleep between them. Use a 200ms timeout. Wait 300ms total. Verify 2 success notifications.
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/nanobot_auto_update" && go test ./internal/telegram/... -run "TestMonitor_Rapid|TestMonitor_Notification|TestMonitor_Disabled|TestMonitor_ContextCancelled|TestMonitor_Empty|TestMonitor_Concurrent" -v -count=1 2>&1 | head -30 (expect failures — RED phase)</automated>
  </verify>
  <done>
    - 8 new test functions appended to monitor_test.go
    - panicNotifier mock type added for panic recovery test
    - New tests compile (existing monitor.go is sufficient for compilation)
    - Some tests may already pass, some may fail — goal is identifying edge case gaps
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Fix any edge case failures and validate with race detector</name>
  <files>internal/telegram/monitor.go</files>

  <read_first>
    - internal/telegram/monitor.go (current implementation to fix)
    - internal/telegram/monitor_test.go (all tests including new edge case tests)
  </read_first>

  <action>
Run the full test suite to see which new edge case tests pass and which fail:

```bash
go test ./internal/telegram/... -v -count=1
```

For any failing tests, fix the implementation in monitor.go. Likely fixes:

1. If TestMonitor_RapidTriggerSuccessSequence fails: The state may not be returning to stateIdle after success. Verify that after IsSuccess detection, state is set to stateIdle (not stateResolved) so the second trigger is detected.

2. If TestMonitor_TriggerFollowedByAnotherTrigger fails: startTimer() may not be stopping the old timer. Verify startTimer calls m.timer.Stop() before creating new timer. Also verify that a trigger arriving in stateWaiting re-enters startTimer (the switch case should handle stateWaiting trigger too, or state should return to idle between cycles).

3. If TestMonitor_NotificationPanicRecovery fails: sendNotification may not have panic recovery. Add defer-recover block to sendNotification.

4. If TestMonitor_ConcurrentTimerAndProcessEntry shows data race: Ensure ALL state access in processEntry and startTimer and the AfterFunc callback are under m.mu.Lock(). Common gap: the startTime field or the timer field accessed without mutex.

5. If TestMonitor_ContextCancelledBeforeTrigger shows goroutine leak: Ensure Start() returns when ctx.Done() fires, and Unsubscribe is called via defer.

After fixes, run with race detector:
```bash
go test ./internal/telegram/... -race -count=1 -v
```

All tests must pass with zero race conditions.

If all tests already pass without changes, no code modification needed — just verify with race detector.
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/nanobot_auto_update" && go test ./internal/telegram/... -race -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/telegram/... -race -count=1 -v` exits 0
    - All test functions pass (original 10 + new 8 = 18 tests)
    - Zero "DATA RACE" messages in output
    - TestMonitor_NotificationPanicRecovery passes (panic recovered, monitor survives)
    - TestMonitor_RapidTriggerSuccessSequence passes (2 success notifications, no timeout)
    - TestMonitor_ConcurrentTimerAndProcessEntry passes (exactly 1 notification)
  </acceptance_criteria>
  <done>
    - All 18+ tests pass
    - Race detector reports zero data races
    - Notification panic recovery verified
    - Rapid state transitions handled correctly
    - No goroutine leaks from cancelled context
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Timer goroutine <-> processEntry goroutine | Concurrent access to monitor state. Protected by mutex (validated by race detector). |
| Notification goroutine <-> Notifier | Panic in Notify() must not crash monitor. Validated by TestMonitor_NotificationPanicRecovery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Denial of Service | sendNotification() | mitigate | Panic recovery with defer-recover in sendNotification prevents notification failure from crashing the monitor goroutine. Validated by TestMonitor_NotificationPanicRecovery. |
| T-42-06 | Denial of Service | startTimer() | mitigate | Timer stopped before creating new one prevents timer accumulation. Validated by TestMonitor_TriggerFollowedByAnotherTrigger. |
</threat_model>

<verification>
```bash
cd "C:/WorkSpace/agent/nanobot_auto_update" && go test ./internal/telegram/... -race -count=1 -v
```

Also verify no regression in full project test suite:
```bash
cd "C:/WorkSpace/agent/nanobot_auto_update" && go test ./... -count=1
```
</verification>

<success_criteria>
- All 18+ tests pass with `go test ./internal/telegram/... -race -count=1 -v`
- Zero data races detected
- Panic in Notify() does not crash monitor
- Full project test suite `go test ./... -count=1` passes
- monitor.go has no new files (only modifications to existing Plan 01 files)
</success_criteria>

<output>
After completion, create `.planning/phases/42-telegram-monitor-core/42-02-SUMMARY.md`
</output>
