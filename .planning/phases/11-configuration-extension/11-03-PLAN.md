---
phase: 11-configuration-extension
plan: 03
type: tdd
wave: 2
depends_on: [11-02]
files_modified: [internal/config/config.go, cmd/nanobot-auto-updater/main.go]
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, CONF-05, CONF-06, SEC-03]
user_setup: []
must_haves:
  truths:
    - "用户可以通过 YAML 配置 API 端口、Token、超时" (CONF-02, CONF-03, CONF-05)
    - "用户可以通过 YAML 配置监控间隔和超时" (CONF-04, CONF-05)
    - "可选配置项有合理默认值，应用可在最少配置下启动" (CONF-01, CONF-02, CONF-04, CONF-05)
    - "必需配置项缺失时，应用拒绝启动并返回明确错误" (CONF-06, SEC-03)
    - "配置加载成功后，日志记录配置值但不记录完整 Token" (SEC-02)
  artifacts:
    - path: "internal/config/config.go"
      provides: "扩展的 Config 结构体，集成 API 和 Monitor 配置"
      contains: "API.*APIConfig"
      contains: "Monitor.*MonitorConfig"
    - path: "cmd/nanobot-auto-updater/main.go"
      provides: "启动时配置验证错误处理，提供清晰的错误指导"
      pattern: "Configuration error"
  key_links:
    - from: "internal/config/config.go"
      to: "internal/config/api.go"
      via: "Config.API field"
      pattern: "API.*APIConfig"
    - from: "internal/config/config.go"
      to: "internal/config/monitor.go"
      via: "Config.Monitor field"
      pattern: "Monitor.*MonitorConfig"
    - from: "cmd/nanobot-auto-updater/main.go"
      to: "internal/config"
      via: "config.Load()"
      pattern: "Configuration error.*os.Exit(1)"
---

<objective>
将 APIConfig 和 MonitorConfig 集成到主配置结构体，实现启动时验证

Purpose: 完成配置系统集成，确保应用启动时验证所有配置项
Output: 可工作的完整配置系统 + 集成测试通过
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-configuration-extension/11-RESEARCH.md

## 现有 Config 结构体

From internal/config/config.go:
```go
type Config struct {
    Cron      string           `yaml:"cron" mapstructure:"cron"`
    Nanobot   NanobotConfig    `yaml:"nanobot" mapstructure:"nanobot"`
    Instances []InstanceConfig `yaml:"instances" mapstructure:"instances"`
    Pushover  PushoverConfig   `yaml:"pushover" mapstructure:"pushover"`
}

func (c *Config) defaults() {
    c.Cron = "0 3 * * *"
    c.Nanobot.RepoPath = ""
    c.Pushover.ApiToken = ""
    c.Pushover.UserKey = ""
}

func (c *Config) Validate() error {
    var errs []error

    // Validate cron expression
    if err := ValidateCron(c.Cron); err != nil {
        errs = append(errs, err)
    }

    // Check mode compatibility
    if err := c.ValidateModeCompatibility(); err != nil {
        errs = append(errs, err)
    }

    // ... instance validation ...

    return errors.Join(errs...)
}
```

## 现有 Load() 函数

From internal/config/config.go:
```go
func Load(configPath string) (*Config, error) {
    v := viper.New()
    v.SetConfigFile(configPath)
    v.SetConfigType("yaml")

    // Create config with defaults
    cfg := New()

    // Set defaults in viper
    v.SetDefault("cron", cfg.Cron)
    v.SetDefault("nanobot.repo_path", cfg.Nanobot.RepoPath)
    v.SetDefault("pushover.api_token", cfg.Pushover.ApiToken)
    v.SetDefault("pushover.user_key", cfg.Pushover.UserKey)

    // Read config file
    if err := v.ReadInConfig(); err != nil {
        if _, ok := err.(viper.ConfigFileNotFoundError); !ok {
            return nil, fmt.Errorf("failed to read config file: %w", err)
        }
    }

    // Unmarshal to struct
    if err := v.Unmarshal(cfg); err != nil {
        return nil, fmt.Errorf("failed to unmarshal config: %w", err)
    }

    // Validate
    if err := cfg.Validate(); err != nil {
        return nil, fmt.Errorf("config validation failed: %w", err)
    }

    return cfg, nil
}
```

## 默认值策略（来自 RESEARCH.md）

- API.Port: 8080 (有默认值)
- API.BearerToken: "" (必需，无默认值)
- API.Timeout: 30s (有默认值)
- Monitor.Interval: 15m (有默认值)
- Monitor.Timeout: 10s (有默认值)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Config struct with new fields and implement integration tests</name>
  <files>internal/config/config.go</files>
  <read_first>
    - internal/config/config.go (现有 Config 结构体)
    - internal/config/api.go (刚实现的 APIConfig)
    - internal/config/monitor.go (刚实现的 MonitorConfig)
    - .planning/phases/11-configuration-extension/11-RESEARCH.md (默认值策略)
    - internal/config/config_test.go (Wave 0 创建的集成测试存根)
  </read_first>
  <behavior>
    - Test 1: Config struct contains API and Monitor fields
    - Test 2: Load() applies defaults for new fields
    - Test 3: Validate() calls API.Validate() and Monitor.Validate()
    - Test 4: Multiple validation errors are aggregated
    - Test 5: Loading valid YAML populates new fields (使用 Wave 0 创建的测试数据)
  </behavior>
  <action>
    Follow TDD RED→GREEN cycle:

    **RED Phase:**
    1. Run integration tests: `go test -v ./internal/config/ -run "TestLoadAPI|TestLoadMonitor|TestConfigValidationWithNew"`
    2. Verify tests fail or skip (这些测试存根在 Wave 0 的 Plan 11-01b Task 5 中创建)

    **GREEN Phase - Modify internal/config/config.go:**

    1. Add new fields to Config struct (after Pushover field):
    ```go
    type Config struct {
        Cron      string           `yaml:"cron" mapstructure:"cron"`
        Nanobot   NanobotConfig    `yaml:"nanobot" mapstructure:"nanobot"`
        Instances []InstanceConfig `yaml:"instances" mapstructure:"instances"`
        Pushover  PushoverConfig   `yaml:"pushover" mapstructure:"pushover"`
        API       APIConfig        `yaml:"api" mapstructure:"api"`           // 新增 (CONF-02, CONF-03)
        Monitor   MonitorConfig    `yaml:"monitor" mapstructure:"monitor"`   // 新增 (CONF-04, CONF-05)
    }
    ```

    2. Update defaults() method to set new default values:
    ```go
    func (c *Config) defaults() {
        c.Cron = "0 3 * * *"
        c.Nanobot.RepoPath = ""
        c.Pushover.ApiToken = ""
        c.Pushover.UserKey = ""

        // 新增默认值 (CONF-01, CONF-02, CONF-04, CONF-05)
        c.API.Port = 8080
        c.API.BearerToken = "" // 必需，无默认值 (SEC-03)
        c.API.Timeout = 30 * time.Second
        c.Monitor.Interval = 15 * time.Minute
        c.Monitor.Timeout = 10 * time.Second
    }
    ```

    3. Update Validate() method to call new validation methods:
    ```go
    func (c *Config) Validate() error {
        var errs []error

        // 现有验证
        if err := ValidateCron(c.Cron); err != nil {
            errs = append(errs, err)
        }
        if err := c.ValidateModeCompatibility(); err != nil {
            errs = append(errs, err)
        }

        // ... 现有实例验证逻辑 ...

        // 新增验证 (CONF-06, SEC-03)
        if err := c.API.Validate(); err != nil {
            errs = append(errs, err)
        }
        if err := c.Monitor.Validate(); err != nil {
            errs = append(errs, err)
        }

        return errors.Join(errs...)
    }
    ```

    4. Update Load() function to set Viper defaults for new fields:
    ```go
    func Load(configPath string) (*Config, error) {
        v := viper.New()
        v.SetConfigFile(configPath)
        v.SetConfigType("yaml")

        cfg := New()

        // 现有默认值
        v.SetDefault("cron", cfg.Cron)
        v.SetDefault("nanobot.repo_path", cfg.Nanobot.RepoPath)
        v.SetDefault("pushover.api_token", cfg.Pushover.ApiToken)
        v.SetDefault("pushover.user_key", cfg.Pushover.UserKey)

        // 新增默认值
        v.SetDefault("api.port", cfg.API.Port)
        v.SetDefault("api.timeout", cfg.API.Timeout)
        v.SetDefault("monitor.interval", cfg.Monitor.Interval)
        v.SetDefault("monitor.timeout", cfg.Monitor.Timeout)

        // Read config file
        if err := v.ReadInConfig(); err != nil {
            if _, ok := err.(viper.ConfigFileNotFoundError); !ok {
                return nil, fmt.Errorf("failed to read config file: %w", err)
            }
        }

        // Unmarshal to struct
        if err := v.Unmarshal(cfg); err != nil {
            return nil, fmt.Errorf("failed to unmarshal config: %w", err)
        }

        // Validate
        if err := cfg.Validate(); err != nil {
            return nil, fmt.Errorf("config validation failed: %w", err)
        }

        return cfg, nil
    }
    ```

    **GREEN Phase - Implement integration tests in config_test.go:**
    ```go
    func TestLoadAPIConfigFromYAML(t *testing.T) {
        cfg, err := Load("../../testutil/testdata/config/api_valid.yaml")
        if err != nil {
            t.Fatalf("Load failed: %v", err)
        }

        // Verify API config loaded
        if cfg.API.Port != 8080 {
            t.Errorf("expected API.Port 8080, got %d", cfg.API.Port)
        }
        if len(cfg.API.BearerToken) < 32 {
            t.Errorf("expected BearerToken length >= 32, got %d", len(cfg.API.BearerToken))
        }
        if cfg.API.Timeout != 30*time.Second {
            t.Errorf("expected API.Timeout 30s, got %v", cfg.API.Timeout)
        }
    }

    func TestLoadMonitorConfigFromYAML(t *testing.T) {
        cfg, err := Load("../../testutil/testdata/config/monitor_valid.yaml")
        if err != nil {
            t.Fatalf("Load failed: %v", err)
        }

        // Verify Monitor config loaded
        if cfg.Monitor.Interval != 15*time.Minute {
            t.Errorf("expected Monitor.Interval 15m, got %v", cfg.Monitor.Interval)
        }
        if cfg.Monitor.Timeout != 10*time.Second {
            t.Errorf("expected Monitor.Timeout 10s, got %v", cfg.Monitor.Timeout)
        }
    }

    func TestConfigValidationWithNewFields(t *testing.T) {
        t.Run("invalid API config rejected", func(t *testing.T) {
            _, err := Load("../../testutil/testdata/config/api_invalid_token.yaml")
            if err == nil {
                t.Fatal("expected error for invalid token, got nil")
            }
            if !strings.Contains(err.Error(), "bearer_token must be at least 32 characters") {
                t.Errorf("error should mention token length, got: %v", err)
            }
        })

        t.Run("invalid Monitor config rejected", func(t *testing.T) {
            _, err := Load("../../testutil/testdata/config/monitor_invalid_interval.yaml")
            if err == nil {
                t.Fatal("expected error for invalid interval, got nil")
            }
            if !strings.Contains(err.Error(), "monitor.interval must be at least 1 minute") {
                t.Errorf("error should mention interval minimum, got: %v", err)
            }
        })

        t.Run("multiple errors aggregated", func(t *testing.T) {
            cfg := New()
            cfg.API.Port = 70000  // Invalid port
            cfg.API.BearerToken = "short"  // Invalid token
            cfg.Monitor.Interval = 30 * time.Second  // Invalid interval

            err := cfg.Validate()
            if err == nil {
                t.Fatal("expected multiple validation errors, got nil")
            }

            // Verify all 3 errors are present
            errMsg := err.Error()
            if !strings.Contains(errMsg, "api.port") {
                t.Error("error should mention api.port")
            }
            if !strings.Contains(errMsg, "bearer_token") {
                t.Error("error should mention bearer_token")
            }
            if !strings.Contains(errMsg, "monitor.interval") {
                t.Error("error should mention monitor.interval")
            }
        })
    }
    ```

    **GREEN Phase - Run tests:**
    5. Run tests: `go test -v ./internal/config/`
    6. All tests should pass

    **Commit:**
    7. Commit with message: `feat(11): integrate API and Monitor config into main Config`
  </action>
  <verify>
    <automated>go test -v ./internal/config/</automated>
  </verify>
  <done>
    - Config struct contains API and Monitor fields
    - defaults() sets new default values
    - Validate() calls API.Validate() and Monitor.Validate()
    - Load() sets Viper defaults for new fields
    - Wave 0 创建的集成测试存根已实现并通过
    - Code committed to git
  </done>
</task>

<task type="auto">
  <name>Task 2: Update main.go startup validation error handling</name>
  <files>cmd/nanobot-auto-updater/main.go</files>
  <read_first>
    - cmd/nanobot-auto-updater/main.go (现有错误处理)
    - .planning/phases/11-configuration-extension/11-RESEARCH.md (错误处理示例)
  </read_first>
  <action>
    Update cmd/nanobot-auto-updater/main.go to provide clear error messages for configuration validation failures:

    **Modify the config loading section (around line 75-80):**

    Current code:
    ```go
    // Load configuration
    cfg, err := config.Load(*configFile)
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error loading config: %v\n", err)
        os.Exit(1)
    }
    ```

    Replace with:
    ```go
    // Load configuration with validation (CONF-06)
    cfg, err := config.Load(*configFile)
    if err != nil {
        fmt.Fprintf(os.Stderr, "Configuration error: %v\n", err)
        fmt.Fprintf(os.Stderr, "\nPlease check your config.yaml file.\n")
        fmt.Fprintf(os.Stderr, "Required fields:\n")
        fmt.Fprintf(os.Stderr, "  - api.bearer_token (at least 32 characters for security)\n")
        fmt.Fprintf(os.Stderr, "Optional fields (have defaults):\n")
        fmt.Fprintf(os.Stderr, "  - api.port (default: 8080)\n")
        fmt.Fprintf(os.Stderr, "  - api.timeout (default: 30s)\n")
        fmt.Fprintf(os.Stderr, "  - monitor.interval (default: 15m)\n")
        fmt.Fprintf(os.Stderr, "  - monitor.timeout (default: 10s)\n")
        fmt.Fprintf(os.Stderr, "  - pushover.api_token (optional, for notifications)\n")
        fmt.Fprintf(os.Stderr, "  - pushover.user_key (optional, for notifications)\n")
        os.Exit(1)
    }
    ```

    **Add log message after successful config load (around line 90):**

    Add after config.Load() succeeds:
    ```go
    // Initialize logging
    logger := logging.NewLogger("./logs")
    slog.SetDefault(logger)

    // Log configuration loaded (CONF-06, SEC-02)
    // Note: Do NOT log full Bearer Token for security
    slog.Info("Configuration loaded and validated",
        "api_port", cfg.API.Port,
        "api_timeout", cfg.API.Timeout,
        "monitor_interval", cfg.Monitor.Interval,
        "monitor_timeout", cfg.Monitor.Timeout,
        "bearer_token_configured", cfg.API.BearerToken != "",
        "bearer_token_length", len(cfg.API.BearerToken),
    )
    ```

    This ensures:
    - Clear error messages when config validation fails (CONF-06)
    - Helpful guidance on required vs optional fields
    - Pushover fields explicitly marked as optional (optional, for notifications)
    - Logging of loaded config (without full token for SEC-02)
  </action>
  <verify>
    <automated>grep -n "Configuration error" cmd/nanobot-auto-updater/main.go</automated>
  </verify>
  <done>
    - main.go contains updated error handling with clear messages
    - Error message lists required fields (api.bearer_token)
    - Error message lists optional fields with defaults
    - Pushover fields explicitly marked as optional (optional, for notifications)
    - Successful load logs config values (without full token)
    - Bearer Token length logged, not content (SEC-02)
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify integration</name>
  <files>internal/config/config_test.go (验证)</files>
  <read_first>
    - internal/config/config_test.go (所有测试)
  </read_first>
  <action>
    Run comprehensive test suite to verify all integration:

    1. Run all config tests:
    ```bash
    go test -v ./internal/config/
    ```

    2. Verify test output includes:
    - All existing tests pass (TestNewConfigDefaults, TestValidateCron, etc.)
    - New API tests pass (TestAPIConfigValidate, TestLoadAPIConfigFromYAML)
    - New Monitor tests pass (TestMonitorConfigValidate, TestLoadMonitorConfigFromYAML)
    - New integration tests pass (TestConfigValidationWithNewFields)

    3. Run tests with coverage:
    ```bash
    go test -v -cover ./internal/config/
    ```

    4. Verify coverage report shows:
    - api.go: ~100% coverage (all validation paths)
    - monitor.go: ~100% coverage (all validation paths)
    - config.go: Increased coverage for Validate() and Load()

    5. Run full project test suite:
    ```bash
    go test -v ./...
    ```

    6. Verify no tests fail across entire project

    If any tests fail:
    - Review test output for specific failures
    - Fix issues in implementation
    - Re-run tests until all pass

    **Manual verification (optional but recommended):**
    7. Create a test config.yaml with invalid bearer token:
    ```yaml
    cron: "0 3 * * *"
    api:
      bearer_token: "too-short"
    ```

    8. Try to run the application:
    ```bash
    go run ./cmd/nanobot-auto-updater --config test_config.yaml
    ```

    9. Verify error message shows clear validation error

    **Commit:**
    10. Commit with message: `test(11): verify full config integration`
  </action>
  <verify>
    <automated>go test -v ./internal/config/ 2>&1 | grep -E "PASS|FAIL" | grep -c PASS</automated>
  </verify>
  <done>
    - All config tests pass
    - Test coverage shows high coverage for new files
    - Full project test suite passes
    - Manual verification confirms error messages are clear
    - Code committed to git
  </done>
</task>

</tasks>

<verification>
Phase 11 完成标准:
- Config 结构体包含所有新字段
- 默认值正确设置
- 验证逻辑完整
- Wave 0 创建的集成测试全部通过
- 启动错误处理清晰
- 所有需求已满足 (CONF-01~06, SEC-03)
</verification>

<success_criteria>
1. internal/config/config.go 包含 API 和 Monitor 字段
2. defaults() 设置所有新默认值
3. Validate() 调用所有子验证
4. Load() 正确解析 YAML 并验证
5. main.go 提供清晰的配置错误消息
6. Pushover 字段明确标注为可选（可选，用于通知）
7. 所有测试通过: `go test -v ./internal/config/`
8. 集成测试验证:
   - 有效配置可以加载
   - 无效配置被拒绝 (Token < 32 chars, Port > 65535, Interval < 1m)
   - 多个错误被聚合
9. 满足所有 Phase 11 需求:
   - CONF-01: Pushover in YAML ✓
   - CONF-02: API port in YAML ✓
   - CONF-03: Bearer Token in YAML ✓
   - CONF-04: Monitor interval in YAML ✓
   - CONF-05: HTTP timeout in YAML ✓
   - CONF-06: Startup validation ✓
   - SEC-03: Token length validation ✓
</success_criteria>

<output>
After completion, create `.planning/phases/11-configuration-extension/11-03-SUMMARY.md`
</output>
