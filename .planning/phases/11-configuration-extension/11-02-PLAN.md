---
phase: 11-configuration-extension
plan: 02
type: tdd
wave: 1
depends_on: [11-01]
files_modified: [internal/config/api.go, internal/config/monitor.go]
autonomous: true
requirements: [CONF-02, CONF-03, CONF-04, CONF-05, SEC-03]
user_setup: []
must_haves:
  truths:
    - "APIConfig 结构体可以正确验证端口、Token、超时"
    - "MonitorConfig 结构体可以正确验证间隔、超时"
    - "所有验证错误消息清晰明确"
    - "Bearer Token 长度验证符合 SEC-03 要求"
  artifacts:
    - path: "internal/config/api.go"
      provides: "APIConfig 结构体 + Validate() 方法"
      exports: ["APIConfig", "Validate"]
      min_lines: 60
    - path: "internal/config/monitor.go"
      provides: "MonitorConfig 结构体 + Validate() 方法"
      exports: ["MonitorConfig", "Validate"]
      min_lines: 50
  key_links:
    - from: "internal/config/api_test.go"
      to: "internal/config/api.go"
      via: "test execution"
      pattern: "TestAPIConfigValidate.*passes"
    - from: "internal/config/monitor_test.go"
      to: "internal/config/monitor.go"
      via: "test execution"
      pattern: "TestMonitorConfigValidate.*passes"
---

<objective>
创建 APIConfig 和 MonitorConfig 结构体，实现验证逻辑，确保测试通过

Purpose: 实现配置验证核心逻辑，遵循 TDD RED→GREEN 循环
Output: 可工作的配置验证代码 + 通过的测试
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-configuration-extension/11-RESEARCH.md

## 现有配置结构体模式

From internal/config/config.go:
- 结构体字段使用 yaml + mapstructure 标签
- Validate() 方法返回 error
- 错误消息使用 fmt.Errorf 格式化，包含具体值
- 时间类型使用 time.Duration

From internal/config/config.go NanobotConfig:
```go
type NanobotConfig struct {
    Port           uint32        `yaml:"port" mapstructure:"port"`
    StartupTimeout time.Duration `yaml:"startup_timeout" mapstructure:"startup_timeout"`
    RepoPath       string        `yaml:"repo_path" mapstructure:"repo_path"`
}

func (nc *NanobotConfig) Validate() error {
    if nc.Port == 0 || nc.Port > 65535 {
        return fmt.Errorf("port must be > 0 and <= 65535, got %d", nc.Port)
    }
    if nc.StartupTimeout < 5*time.Second {
        return fmt.Errorf("startup_timeout must be at least 5 seconds, got %v", nc.StartupTimeout)
    }
    return nil
}
```

## 验证规则（来自 RESEARCH.md）

APIConfig:
- Port: 1-65535 (uint32)
- BearerToken: ≥ 32 字符 (SEC-03)
- Timeout: ≥ 5 秒 (time.Duration)

MonitorConfig:
- Interval: ≥ 1 分钟 (time.Duration)
- Timeout: ≥ 1 秒 (time.Duration)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement APIConfig with TDD</name>
  <files>internal/config/api.go</files>
  <read_first>
    - internal/config/config.go (现有结构体模式)
    - internal/config/api_test.go (Wave 0 创建的测试)
    - .planning/phases/11-configuration-extension/11-RESEARCH.md (APIConfig 规范)
  </read_first>
  <behavior>
    - Test 1: Valid config with port=8080, token=32 chars, timeout=30s passes validation
    - Test 2: Port=0 fails with error "api.port must be between 1 and 65535, got 0"
    - Test 3: Port=65536 fails with error "api.port must be between 1 and 65535, got 65536"
    - Test 4: Token with 31 chars fails with error "api.bearer_token must be at least 32 characters for security, got 31"
    - Test 5: Timeout=4s fails with error "api.timeout must be at least 5 seconds, got 4s"
  </behavior>
  <action>
    Follow TDD RED→GREEN cycle:

    **RED Phase:**
    1. Run tests to see failures: `go test -v ./internal/config/ -run TestAPIConfigValidate`
    2. Verify all 5 tests fail or skip

    **GREEN Phase - Create internal/config/api.go:**
    ```go
    package config

    import (
        "fmt"
        "time"
    )

    // APIConfig holds configuration for HTTP API server.
    type APIConfig struct {
        Port        uint32        `yaml:"port" mapstructure:"port"`
        BearerToken string        `yaml:"bearer_token" mapstructure:"bearer_token"`
        Timeout     time.Duration `yaml:"timeout" mapstructure:"timeout"`
    }

    // Validate validates the APIConfig values.
    func (ac *APIConfig) Validate() error {
        // Port validation
        if ac.Port == 0 || ac.Port > 65535 {
            return fmt.Errorf("api.port must be between 1 and 65535, got %d", ac.Port)
        }

        // Bearer Token validation (SEC-03)
        if len(ac.BearerToken) < 32 {
            return fmt.Errorf("api.bearer_token must be at least 32 characters for security, got %d", len(ac.BearerToken))
        }

        // Timeout validation
        if ac.Timeout < 5*time.Second {
            return fmt.Errorf("api.timeout must be at least 5 seconds, got %v", ac.Timeout)
        }

        return nil
    }
    ```

    **Remove t.Skip() from api_test.go:**
    - Remove all `t.Skip("Waiting for api.go implementation")` lines
    - Implement actual test logic:

    ```go
    func TestAPIConfigValidate(t *testing.T) {
        t.Run("valid config", func(t *testing.T) {
            cfg := APIConfig{
                Port:        8080,
                BearerToken: "this-is-a-secure-token-with-at-least-32-chars",
                Timeout:     30 * time.Second,
            }
            if err := cfg.Validate(); err != nil {
                t.Errorf("expected valid config, got error: %v", err)
            }
        })

        t.Run("invalid port", func(t *testing.T) {
            testCases := []struct {
                name string
                port uint32
            }{
                {"zero", 0},
                {"too large", 65536},
            }

            for _, tc := range testCases {
                t.Run(tc.name, func(t *testing.T) {
                    cfg := APIConfig{
                        Port:        tc.port,
                        BearerToken: "this-is-a-secure-token-with-at-least-32-chars",
                        Timeout:     30 * time.Second,
                    }
                    err := cfg.Validate()
                    if err == nil {
                        t.Error("expected error for invalid port, got nil")
                    }
                    if !strings.Contains(err.Error(), "api.port must be between 1 and 65535") {
                        t.Errorf("error message should contain port validation, got: %v", err)
                    }
                })
            }
        })

        t.Run("bearer token too short", func(t *testing.T) {
            cfg := APIConfig{
                Port:        8080,
                BearerToken: "too-short-token-31-chars-xxxx",
                Timeout:     30 * time.Second,
            }
            err := cfg.Validate()
            if err == nil {
                t.Error("expected error for short token, got nil")
            }
            if !strings.Contains(err.Error(), "api.bearer_token must be at least 32 characters for security") {
                t.Errorf("error message should contain token length validation, got: %v", err)
            }
        })

        t.Run("timeout too short", func(t *testing.T) {
            cfg := APIConfig{
                Port:        8080,
                BearerToken: "this-is-a-secure-token-with-at-least-32-chars",
                Timeout:     4 * time.Second,
            }
            err := cfg.Validate()
            if err == nil {
                t.Error("expected error for short timeout, got nil")
            }
            if !strings.Contains(err.Error(), "api.timeout must be at least 5 seconds") {
                t.Errorf("error message should contain timeout validation, got: %v", err)
            }
        })
    }
    ```

    **GREEN Phase - Run tests:**
    3. Run tests: `go test -v ./internal/config/ -run TestAPIConfigValidate`
    4. All 5 tests should pass

    **Commit:**
    5. Commit with message: `feat(11): implement APIConfig validation`
  </action>
  <verify>
    <automated>go test -v ./internal/config/ -run TestAPIConfigValidate</automated>
  </verify>
  <done>
    - File internal/config/api.go exists with APIConfig struct and Validate() method
    - All validation rules implemented:
      - Port: 1-65535
      - BearerToken: ≥ 32 chars (SEC-03)
      - Timeout: ≥ 5s
    - Error messages include actual values
    - All tests in api_test.go pass
    - Code committed to git
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MonitorConfig with TDD</name>
  <files>internal/config/monitor.go</files>
  <read_first>
    - internal/config/api.go (刚实现的结构体模式)
    - internal/config/monitor_test.go (Wave 0 创建的测试)
    - .planning/phases/11-configuration-extension/11-RESEARCH.md (MonitorConfig 规范)
  </read_first>
  <behavior>
    - Test 1: Valid config with interval=15m, timeout=10s passes validation
    - Test 2: Interval=30s fails with error "monitor.interval must be at least 1 minute"
    - Test 3: Timeout=500ms fails with error "monitor.timeout must be at least 1 second"
  </behavior>
  <action>
    Follow TDD RED→GREEN cycle:

    **RED Phase:**
    1. Run tests: `go test -v ./internal/config/ -run TestMonitorConfigValidate`
    2. Verify all 3 tests fail or skip

    **GREEN Phase - Create internal/config/monitor.go:**
    ```go
    package config

    import (
        "fmt"
        "time"
    )

    // MonitorConfig holds configuration for monitoring service.
    type MonitorConfig struct {
        Interval time.Duration `yaml:"interval" mapstructure:"interval"` // Google 连通性检查间隔
        Timeout  time.Duration `yaml:"timeout" mapstructure:"timeout"`   // HTTP 请求超时
    }

    // Validate validates the MonitorConfig values.
    func (mc *MonitorConfig) Validate() error {
        // Interval validation
        if mc.Interval < 1*time.Minute {
            return fmt.Errorf("monitor.interval must be at least 1 minute, got %v", mc.Interval)
        }

        // Timeout validation
        if mc.Timeout < 1*time.Second {
            return fmt.Errorf("monitor.timeout must be at least 1 second, got %v", mc.Timeout)
        }

        return nil
    }
    ```

    **Remove t.Skip() from monitor_test.go and implement tests:**
    ```go
    func TestMonitorConfigValidate(t *testing.T) {
        t.Run("valid config", func(t *testing.T) {
            cfg := MonitorConfig{
                Interval: 15 * time.Minute,
                Timeout:  10 * time.Second,
            }
            if err := cfg.Validate(); err != nil {
                t.Errorf("expected valid config, got error: %v", err)
            }
        })

        t.Run("interval too short", func(t *testing.T) {
            cfg := MonitorConfig{
                Interval: 30 * time.Second,
                Timeout:  10 * time.Second,
            }
            err := cfg.Validate()
            if err == nil {
                t.Error("expected error for short interval, got nil")
            }
            if !strings.Contains(err.Error(), "monitor.interval must be at least 1 minute") {
                t.Errorf("error message should contain interval validation, got: %v", err)
            }
        })

        t.Run("timeout too short", func(t *testing.T) {
            cfg := MonitorConfig{
                Interval: 15 * time.Minute,
                Timeout:  500 * time.Millisecond,
            }
            err := cfg.Validate()
            if err == nil {
                t.Error("expected error for short timeout, got nil")
            }
            if !strings.Contains(err.Error(), "monitor.timeout must be at least 1 second") {
                t.Errorf("error message should contain timeout validation, got: %v", err)
            }
        })
    }
    ```

    **GREEN Phase - Run tests:**
    3. Run tests: `go test -v ./internal/config/ -run TestMonitorConfigValidate`
    4. All 3 tests should pass

    **Commit:**
    5. Commit with message: `feat(11): implement MonitorConfig validation`
  </action>
  <verify>
    <automated>go test -v ./internal/config/ -run TestMonitorConfigValidate</automated>
  </verify>
  <done>
    - File internal/config/monitor.go exists with MonitorConfig struct and Validate() method
    - All validation rules implemented:
      - Interval: ≥ 1m
      - Timeout: ≥ 1s
    - Error messages include actual values
    - All tests in monitor_test.go pass
    - Code committed to git
  </done>
</task>

</tasks>

<verification>
Wave 1 完成标准:
- APIConfig 和 MonitorConfig 结构体已实现
- 所有单元测试通过
- 验证逻辑符合 RESEARCH.md 规范
- SEC-03 Bearer Token 长度验证已实现
- 代码已提交
</verification>

<success_criteria>
1. internal/config/api.go 包含 APIConfig + Validate()
2. internal/config/monitor.go 包含 MonitorConfig + Validate()
3. 所有 api_test.go 测试通过 (5 个测试)
4. 所有 monitor_test.go 测试通过 (3 个测试)
5. 运行 `go test -v ./internal/config/` 无失败
6. 代码已提交到 git
</success_criteria>

<output>
After completion, create `.planning/phases/11-configuration-extension/11-02-SUMMARY.md`
</output>
