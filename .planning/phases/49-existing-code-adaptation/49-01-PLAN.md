---
phase: 49-existing-code-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/lifecycle/daemon.go
  - internal/api/selfupdate_handler.go
autonomous: true
requirements: [ADPT-01, ADPT-02, ADPT-03]

must_haves:
  truths:
    - "服务模式下 daemon.go 的 MakeDaemon 和 MakeDaemonSimple 立即返回 (false, nil)，不进入守护进程循环"
    - "服务模式下 defaultRestartFn 调用 os.Exit(1) 触发 SCM recovery policy 重启"
    - "控制台模式下 defaultRestartFn 保持现有 self-spawn 行为 (exec.Command + os.Exit(0))"
    - "服务模式下工作目录已设置为 exe 所在目录 (main.go:74-83 已实现)"
    - "控制台模式下所有行为无回归"
  artifacts:
    - path: "internal/lifecycle/daemon.go"
      provides: "MakeDaemon/MakeDaemonSimple with IsServiceMode guard"
      contains: "IsServiceMode"
    - path: "internal/api/selfupdate_handler.go"
      provides: "defaultRestartFn with service mode detection"
      contains: "IsServiceMode"
  key_links:
    - from: "internal/lifecycle/daemon.go"
      to: "internal/lifecycle/servicedetect_windows.go"
      via: "IsServiceMode() call"
      pattern: "IsServiceMode"
    - from: "internal/api/selfupdate_handler.go"
      to: "internal/lifecycle/servicedetect_windows.go"
      via: "IsServiceMode() call in defaultRestartFn"
      pattern: "IsServiceMode"
---

<objective>
服务模式下守护进程和自更新重启机制的适配。

Purpose: 确保 daemon.go 在服务模式下跳过守护进程循环（SCM 管理进程生命周期），自更新后 restartFn 使用 SCM recovery policy 而非 self-spawn，工作目录适配已正确实现，控制台模式无回归。
Output: 修改后的 daemon.go 和 selfupdate_handler.go
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-existing-code-adaptation/49-CONTEXT.md

<interfaces>
<!-- IsServiceMode — 服务模式检测 -->
From internal/lifecycle/servicedetect_windows.go:
```go
//go:build windows
func IsServiceMode() (bool, error)
```

From internal/lifecycle/servicedetect.go:
```go
//go:build !windows
func IsServiceMode() (bool, error)  // always returns false, nil
```

<!-- daemon.go current signatures (Windows build) -->
From internal/lifecycle/daemon.go:
```go
func MakeDaemon() (bool, error)
func MakeDaemonSimple() (bool, error)
```

<!-- defaultRestartFn current implementation -->
From internal/api/selfupdate_handler.go:
```go
func defaultRestartFn(exePath string) {
    cmd := exec.Command(exePath, os.Args[1:]...)
    cmd.SysProcAttr = &windows.SysProcAttr{
        HideWindow:    true,
        CreationFlags: windows.CREATE_NO_WINDOW | windows.CREATE_NEW_PROCESS_GROUP | windows.DETACHED_PROCESS,
    }
    slog.Info("self-spawn restart initiated", "exe", exePath)
    if err := cmd.Start(); err != nil {
        slog.Error("failed to spawn new process after update", "error", err)
        return
    }
    os.Exit(0)
}
```

<!-- main.go work directory adaptation (ADPT-03, already implemented) -->
From cmd/nanobot-auto-updater/main.go lines 74-83:
```go
if inService {
    if exePath, err := os.Executable(); err == nil {
        if exeDir := filepath.Dir(exePath); exeDir != "" {
            os.Chdir(exeDir)
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: daemon.go 服务模式跳过守护进程 (ADPT-01, D-08)</name>
  <files>internal/lifecycle/daemon.go</files>
  <read_first>
    - internal/lifecycle/daemon.go (当前实现)
    - internal/lifecycle/servicedetect_windows.go (IsServiceMode 签名)
    - .planning/phases/49-existing-code-adaptation/49-CONTEXT.md (D-08 决策)
  </read_first>
  <action>
在 daemon.go 的 MakeDaemon() 函数开头（第 18 行 `if os.Getenv` 之前）添加服务模式检查：

```go
// 服务模式下跳过守护进程模式（ADPT-01, D-08）
// SCM 负责进程生命周期管理，不需要守护进程循环
if isSvc, _ := IsServiceMode(); isSvc {
    return false, nil
}
```

在 MakeDaemonSimple() 函数开头（第 39 行 `if os.Getenv` 之前）添加同样的检查：

```go
// 服务模式下跳过守护进程模式（ADPT-01, D-08）
if isSvc, _ := IsServiceMode(); isSvc {
    return false, nil
}
```

注意：忽略 IsServiceMode 的 error 返回值（用 `_`），因为：
1. 非构建约束场景下已正确处理此错误。
2. 如果检测出错，最安全的做法是继续执行守护进程逻辑（不应改变行为）。
3. 函数有 `//go:build windows` 标签，IsServiceMode 在 Windows 上直接调用 svc.IsWindowsService()。

不需要添加 lifecycle 包的 import（daemon.go 已在 lifecycle 包中，IsServiceMode 是同包函数）。
不需要修改 build tag（daemon.go 已有 `//go:build windows`，servicedetect_windows.go 也有 `//go:build windows`）。
不需要创建 daemon.go 的非 Windows 版本（当前 daemon.go 只有 windows 版本，非 Windows 没有 daemon.go）。
  </action>
  <acceptance_criteria>
    - daemon.go 包含两处 `IsServiceMode()` 调用（MakeDaemon 和 MakeDaemonSimple 各一处）
    - MakeDaemon 中的 IsServiceMode 检查在 `os.Getenv("NANOBOT_UPDATER_DAEMON")` 检查之前
    - MakeDaemonSimple 中的 IsServiceMode 检查在 `os.Getenv("NANOBOT_UPDATER_DAEMON")` 检查之前
    - 两处检查都在返回 `(false, nil)` 之前
    - `go build ./internal/lifecycle/` 编译通过
    - `go vet ./internal/lifecycle/` 无警告
  </acceptance_criteria>
  <verify>
    <automated>cd /c/WorkSpace/agent/nanobot_auto_update && go build ./internal/lifecycle/ && go vet ./internal/lifecycle/</automated>
  </verify>
  <done>daemon.go 中 MakeDaemon 和 MakeDaemonSimple 函数开头均有 IsServiceMode 检查，服务模式下返回 (false, nil)，编译通过</done>
</task>

<task type="auto">
  <name>Task 2: defaultRestartFn 服务模式 SCM 重启 + 验证 ADPT-03 (ADPT-02, D-01/D-02/D-03)</name>
  <files>internal/api/selfupdate_handler.go</files>
  <read_first>
    - internal/api/selfupdate_handler.go (当前 defaultRestartFn 实现在第 83-98 行)
    - internal/lifecycle/servicedetect_windows.go (IsServiceMode 签名)
    - cmd/nanobot-auto-updater/main.go (第 74-83 行工作目录适配，验证 ADPT-03 已实现)
    - .planning/phases/49-existing-code-adaptation/49-CONTEXT.md (D-01/D-02/D-03 决策)
  </read_first>
  <action>
修改 defaultRestartFn 函数（第 83-98 行），在函数开头添加服务模式检测分支（D-01/D-02/D-03）：

替换整个 defaultRestartFn 函数为：

```go
// defaultRestartFn is the production restart implementation.
// Service mode (D-01): exits with code 1 to trigger SCM recovery policy auto-restart.
// Console mode (D-02): spawns a new process and exits with code 0 (self-spawn).
func defaultRestartFn(exePath string) {
    // ADPT-02, D-03: Check service mode to choose restart strategy
    if isSvc, _ := lifecycle.IsServiceMode(); isSvc {
        // D-01: Service mode — exit with non-zero code to trigger SCM recovery policy.
        // Phase 48 configured: 3x ServiceRestart, 60s interval, 24h reset failure count.
        slog.Info("service mode restart: exiting to trigger SCM recovery policy")
        os.Exit(1)
    }

    // D-02: Console mode — original self-spawn behavior (unchanged)
    cmd := exec.Command(exePath, os.Args[1:]...)
    cmd.SysProcAttr = &windows.SysProcAttr{
        HideWindow:    true,
        CreationFlags: windows.CREATE_NO_WINDOW | windows.CREATE_NEW_PROCESS_GROUP | windows.DETACHED_PROCESS,
    }
    // Log before cmd.Start since we won't be around after os.Exit
    slog.Info("self-spawn restart initiated", "exe", exePath)
    if err := cmd.Start(); err != nil {
        slog.Error("failed to spawn new process after update", "error", err)
        return
    }
    os.Exit(0)
}
```

需要在 selfupdate_handler.go 的 import 块中添加 `"github.com/HQGroup/nanobot-auto-updater/internal/lifecycle"`。

注意：
- 忽略 IsServiceMode 的 error（用 `_`），理由同 Task 1
- selfupdate_handler.go 有 `//go:build windows` 标签（需要确认），如果没有，IsServiceMode 在非 Windows 上返回 false（no-op stub），不影响行为
- 不需要修改 lifecycle 包的任何导出接口或类型

同时验证 ADPT-03 (D-09)：确认 main.go 第 74-83 行的工作目录适配代码已正确实现，无需额外改动。验证标准：代码存在且逻辑正确（os.Executable -> filepath.Dir -> os.Chdir）。
  </action>
  <acceptance_criteria>
    - selfupdate_handler.go 的 import 块包含 `"github.com/HQGroup/nanobot-auto-updater/internal/lifecycle"`
    - defaultRestartFn 函数包含 `lifecycle.IsServiceMode()` 调用
    - 服务模式分支调用 `os.Exit(1)`（非零退出码）
    - 控制台模式分支保持 `exec.Command` + `os.Exit(0)` 不变
    - 日志消息包含 "SCM recovery policy" 便于运维排查
    - main.go 第 74-83 行存在 `os.Executable()` + `filepath.Dir` + `os.Chdir` 工作目录适配代码
    - `go build ./cmd/nanobot-auto-updater/` 编译通过
    - `go vet ./internal/api/` 无警告
  </acceptance_criteria>
  <verify>
    <automated>cd /c/WorkSpace/agent/nanobot_auto_update && go build ./cmd/nanobot-auto-updater/ && go vet ./internal/api/</automated>
  </verify>
  <done>defaultRestartFn 在服务模式下调用 os.Exit(1) 触发 SCM recovery policy，控制台模式保持 self-spawn 行为。main.go 工作目录适配已确认正确（ADPT-03 无需额外改动）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| restartFn -> os.Exit | 进程终止点，退出码决定 SCM 行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Denial of Service | defaultRestartFn | accept | 服务模式下 os.Exit(1) 触发 SCM recovery policy 是设计意图，60s 重启间隔足以防止快速崩溃循环。24h 重置计数器防止永久标记失败。 |
| T-49-02 | Tampering | daemon.go IsServiceMode check | accept | IsServiceMode 基于 Windows API (svc.IsWindowsService)，攻击者需要在服务上下文中运行才能绕过，此时守护进程跳过是正确行为。 |
</threat_model>

<verification>
1. `go build ./cmd/nanobot-auto-updater/` 编译通过
2. `go vet ./internal/lifecycle/ ./internal/api/` 无警告
3. daemon.go 中 grep "IsServiceMode" 出现至少 2 次（MakeDaemon + MakeDaemonSimple）
4. selfupdate_handler.go 中 grep "lifecycle.IsServiceMode" 出现 1 次
5. selfupdate_handler.go 中 grep "os.Exit(1)" 出现在服务模式分支
6. selfupdate_handler.go 中控制台模式分支仍包含 "self-spawn" 日志
</verification>

<success_criteria>
- daemon.go MakeDaemon/MakeDaemonSimple 在服务模式下返回 (false, nil)
- defaultRestartFn 服务模式退出码 1（触发 SCM 重启），控制台模式退出码 0（self-spawn）
- 所有编译和静态检查通过
- 控制台模式行为无回归
</success_criteria>

<output>
After completion, create `.planning/phases/49-existing-code-adaptation/49-01-SUMMARY.md`
</output>
