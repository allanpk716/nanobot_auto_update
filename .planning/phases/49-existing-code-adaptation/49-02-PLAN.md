---
phase: 49-existing-code-adaptation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/hotreload.go
  - cmd/nanobot-auto-updater/main.go
  - internal/lifecycle/service_windows.go
  - internal/lifecycle/service.go
autonomous: true
requirements: [ADPT-04]

must_haves:
  truths:
    - "服务模式下修改 config.yaml 后，受影响的组件自动重建，无需重启服务"
    - "实例配置变更（新增/删除/修改）触发对应的实例启动/停止"
    - "监控参数变更触发 NetworkMonitor 重建"
    - "Pushover 配置变更触发 Notifier 重建"
    - "自更新配置变更触发 SelfUpdater 重建"
    - "健康检查参数变更触发 HealthMonitor 重建"
    - "API bearer_token 变更可热更新"
    - "api.port 和 service 配置不热重载"
    - "配置重载失败时保留旧配置继续运行"
    - "控制台模式下 viper.WatchConfig 不启动"
  artifacts:
    - path: "internal/config/hotreload.go"
      provides: "WatchConfig function, OnConfigChange handler, component rebuild logic"
      exports: ["WatchConfig", "StopWatch"]
    - path: "internal/config/config.go"
      provides: "Exported viper instance for WatchConfig usage"
      contains: "viperInstance"
    - path: "cmd/nanobot-auto-updater/main.go"
      provides: "Service mode WatchConfig startup call"
      contains: "config.WatchConfig"
    - path: "internal/lifecycle/service_windows.go"
      provides: "onReady callback in ServiceHandler, called after AppStartup succeeds"
      contains: "onReady"
  key_links:
    - from: "cmd/nanobot-auto-updater/main.go"
      to: "internal/config/hotreload.go"
      via: "config.WatchConfig() call in onReady closure"
      pattern: "config\\.WatchConfig"
    - from: "internal/config/hotreload.go"
      to: "internal/config/config.go"
      via: "shared viper instance"
      pattern: "viperInstance"
    - from: "internal/lifecycle/service_windows.go"
      to: "internal/config/hotreload.go"
      via: "onReady callback invoking WatchConfig after AppStartup reports Running"
      pattern: "onReady"
---

<objective>
服务模式下配置文件热重载。

Purpose: 服务模式下监听 config.yaml 文件变更，自动重载受影响配置并重建对应组件，无需重启服务。遵循 D-04 到 D-07 的决策。
Output: config/hotreload.go 热重载模块，config.go 导出 viper 实例，service_windows.go 添加 onReady 回调，main.go 通过 onReady 启动热重载
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-existing-code-adaptation/49-CONTEXT.md

<interfaces>
<!-- Config struct with all hot-reloadable sections -->
From internal/config/config.go:
```go
type Config struct {
    Instances  []InstanceConfig
    Pushover   PushoverConfig    `yaml:"pushover" mapstructure:"pushover"`
    API        APIConfig         `yaml:"api" mapstructure:"api"`
    Monitor    MonitorConfig     `yaml:"monitor" mapstructure:"monitor"`
    HealthCheck HealthCheckConfig `yaml:"health_check" mapstructure:"health_check"`
    SelfUpdate SelfUpdateConfig  `yaml:"self_update" mapstructure:"self_update"`
    Service    ServiceConfig     `yaml:"service" mapstructure:"service"`
}

func Load(configPath string) (*Config, error)  // creates viper.New() internally
```

<!-- AppComponents holding all components that may be rebuilt -->
From internal/lifecycle/app.go:
```go
type AppComponents struct {
    NotificationManager *notification.NotificationManager
    NetworkMonitor      *network.NetworkMonitor
    HealthMonitor       HealthMonitorControl
    CleanupCron         *cron.Cron
    UpdateLogger        LogScheduler
    APIServer           APIServerControl
    Notifier            NotifySender
    InstanceManager     any  // *instance.InstanceManager
    SelfUpdater         *selfupdate.Updater
    AutoStartDone       chan struct{}
}

func AppStartup(ctx context.Context, c *AppComponents, logger *slog.Logger)
```

<!-- ServiceHandler current struct and RunService signature (Windows) -->
From internal/lifecycle/service_windows.go:
```go
type ServiceHandler struct {
    cfg              *config.Config
    logger           *slog.Logger
    version          string
    updateLogger     LogScheduler
    notif            NotifySender
    createComponents CreateComponentsFunc
    startInstances   StartInstancesFunc
}

func NewServiceHandler(cfg *config.Config, logger *slog.Logger, version string,
    updateLogger LogScheduler, notif NotifySender,
    createComponents CreateComponentsFunc, startInstances StartInstancesFunc) *ServiceHandler

func RunService(cfg *config.Config, logger *slog.Logger, version string,
    updateLogger LogScheduler, notif NotifySender,
    createComponents CreateComponentsFunc, startInstances StartInstancesFunc) error
```

<!-- RunService non-Windows stub -->
From internal/lifecycle/service.go:
```go
//go:build !windows
func RunService(cfg *config.Config, logger *slog.Logger, version string,
    updateLogger LogScheduler, notif NotifySender,
    createComponents CreateComponentsFunc, startInstances StartInstancesFunc) error
    // returns fmt.Errorf("service mode is not supported on this platform")
```

<!-- Component creation patterns -->
From cmd/nanobot-auto-updater/main.go:
```go
// SelfUpdater creation:
selfupdate.NewUpdater(selfupdate.SelfUpdateConfig{GithubOwner: ..., GithubRepo: ...}, logger)

// NetworkMonitor creation:
network.NewNetworkMonitor("https://www.google.com", cfg.Monitor.Interval, cfg.Monitor.Timeout, logger)

// Notifier creation:
notifier.NewWithConfig(notifier.Config{ApiToken: ..., UserKey: ...}, logger)

// NotificationManager creation:
notification.NewNotificationManager(c.NetworkMonitor, notif, logger)
// Started with: go c.NotificationManager.Start(cfg.Monitor.Interval)

// HealthMonitor creation:
health.NewHealthMonitor(statusFunc, cfg.HealthCheck.Interval, logger)
```

<!-- NetworkMonitor has Stop() -->
From internal/network/monitor.go:
```go
func NewNetworkMonitor(url string, interval, timeout time.Duration, logger *slog.Logger) *NetworkMonitor
func (nm *NetworkMonitor) Start()
func (nm *NetworkMonitor) Stop()
```

<!-- NotificationManager has Stop() and Start(interval) -->
From internal/notification/manager.go:
```go
func NewNotificationManager(monitor NetworkMonitor, notifier Notifier, logger *slog.Logger) *NotificationManager
func (nm *NotificationManager) Start(checkInterval time.Duration)
func (nm *NotificationManager) Stop()
```

<!-- HealthMonitor has Start() and Stop() -->
From internal/health/monitor.go:
```go
func NewHealthMonitor(...) *HealthMonitor
func (hm *HealthMonitor) Start()
func (hm *HealthMonitor) Stop()
```

<!-- SelfUpdater is a struct with cfg field -->
From internal/selfupdate/selfupdate.go:
```go
type Updater struct { cfg SelfUpdateConfig; ... }
func NewUpdater(cfg SelfUpdateConfig, logger *slog.Logger) *Updater
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 导出 viper 实例 + 创建 hotreload 模块 (D-04/D-05/D-06/D-07)</name>
  <files>internal/config/config.go, internal/config/hotreload.go</files>
  <read_first>
    - internal/config/config.go (当前 Load 函数和 viper 使用方式)
    - internal/lifecycle/app.go (AppComponents 结构体和组件接口)
    - internal/network/monitor.go (NetworkMonitor 的 Stop/Start 签名)
    - internal/notification/manager.go (NotificationManager 的 Stop/Start 签名)
    - internal/health/monitor.go (HealthMonitor 的 Stop/Start 签名)
    - internal/selfupdate/selfupdate.go (Updater 的 NewUpdater 签名)
    - internal/notifier/notifier.go (Notifier 的 NewWithConfig 签名)
    - .planning/phases/49-existing-code-adaptation/49-CONTEXT.md (D-04 到 D-07 决策)
  </read_first>
  <action>
**Part A: 修改 config.go 导出 viper 实例**

在 config.go 中，将 Load 函数里的局部 viper 实例改为包级别变量，以便 hotreload 模块复用：

1. 在 `Load` 函数外声明包级变量：
```go
// viperInstance holds the viper instance used by Load().
// Exported so that hotreload.go can call WatchConfig/OnConfigChange on the same instance.
var viperInstance *viper.Viper
```

2. 在 `Load` 函数中，将 `v := viper.New()` 改为 `viperInstance = viper.New()` 并将后续的 `v.` 调用全部改为 `viperInstance.`

3. 添加获取 viper 实例的函数：
```go
// GetViper returns the viper instance used for config loading.
// Returns nil if Load() has not been called yet.
func GetViper() *viper.Viper {
    return viperInstance
}
```

4. 添加重新加载配置的函数：
```go
// ReloadConfig re-reads the config file and returns a new Config with validation.
// Returns the new Config on success, or the old config and error on failure.
func ReloadConfig(old *Config) (*Config, error) {
    if viperInstance == nil {
        return old, fmt.Errorf("viper not initialized")
    }
    if err := viperInstance.ReadInConfig(); err != nil {
        return old, fmt.Errorf("failed to re-read config file: %w", err)
    }
    newCfg := New()
    if err := viperInstance.Unmarshal(newCfg); err != nil {
        return old, fmt.Errorf("failed to unmarshal config: %w", err)
    }
    if err := newCfg.Validate(); err != nil {
        return old, fmt.Errorf("config validation failed: %w", err)
    }
    return newCfg, nil
}
```

**Part B: 创建 hotreload.go**

创建 `internal/config/hotreload.go`，实现服务模式下的配置热重载。

关键设计：
- WatchConfig 启动 viper.WatchConfig() 文件监听
- OnConfigChange 回调中：对比新旧配置，识别变更项，调用对应的重建函数
- HotReloadCallbacks 结构体封装各组件的重建回调（避免循环依赖）
- 重建失败时保留旧配置继续运行

```go
package config

import (
    "context"
    "fmt"
    "log/slog"
    "reflect"
    "sync"
    "time"

    "github.com/spf13/viper"
)

// HotReloadCallbacks holds functions to rebuild specific components when their config changes.
// Each function receives the new config and returns nothing.
// Functions are optional (nil-safe) -- if a callback is nil, that config section is skipped.
type HotReloadCallbacks struct {
    // OnMonitorChange rebuilds NetworkMonitor + NotificationManager when monitor config changes.
    // Old components are stopped before calling; caller starts new ones after return.
    OnMonitorChange func(newCfg *Config)

    // OnPushoverChange rebuilds Notifier when pushover config changes.
    OnPushoverChange func(newCfg *Config)

    // OnSelfUpdateChange rebuilds SelfUpdater when self_update config changes.
    OnSelfUpdateChange func(newCfg *Config)

    // OnHealthCheckChange rebuilds HealthMonitor when health_check config changes.
    OnHealthCheckChange func(newCfg *Config)

    // OnBearerTokenChange updates API bearer token when api.bearer_token changes.
    OnBearerTokenChange func(newCfg *Config)

    // OnInstancesChange handles instance config changes (add/remove/modify).
    OnInstancesChange func(newCfg *Config)
}

// hotReloadState manages the config file watcher lifecycle.
type hotReloadState struct {
    mu       sync.Mutex
    viper    *viper.Viper
    logger   *slog.Logger
    current  *Config
    callbacks *HotReloadCallbacks
    running  bool
}

var globalHotReload *hotReloadState

// WatchConfig starts watching the config file for changes (D-04).
// Only call this in service mode. Console mode does not use hot reload.
// The callbacks define how each config section is rebuilt when changed.
func WatchConfig(currentCfg *Config, logger *slog.Logger, callbacks *HotReloadCallbacks) {
    if globalHotReload != nil {
        logger.Warn("config hot reload already active")
        return
    }

    v := GetViper()
    if v == nil {
        logger.Error("cannot start config watch: viper not initialized (Load() not called)")
        return
    }

    state := &hotReloadState{
        viper:     v,
        logger:    logger.With("source", "config-hotreload"),
        current:   currentCfg,
        callbacks: callbacks,
        running:   true,
    }
    globalHotReload = state

    v.OnConfigChange(func(e viper.ConfigChangeInfo) {
        state.mu.Lock()
        defer state.mu.Unlock()

        state.logger.Info("config file changed, reloading",
            "file", e.Name,
            "operation", e.Op,
        )

        newCfg, err := ReloadConfig(state.current)
        if err != nil {
            state.logger.Error("config reload failed, keeping current config",
                "error", err)
            return
        }

        state.handleConfigChange(state.current, newCfg)
        state.current = newCfg
    })

    v.WatchConfig()
    logger.Info("config file watcher started (service mode)")
}

// handleConfigChange compares old and new config, invoking only the callbacks
// for sections that actually changed (D-07).
func (s *hotReloadState) handleConfigChange(oldCfg, newCfg *Config) {
    // D-06: api.port and service config are NOT hot-reloaded
    // Only reload sections listed in D-05

    // Monitor (interval/timeout) -> rebuild NetworkMonitor + NotificationManager
    if !reflect.DeepEqual(oldCfg.Monitor, newCfg.Monitor) {
        s.logger.Info("monitor config changed, triggering rebuild",
            "old_interval", oldCfg.Monitor.Interval,
            "new_interval", newCfg.Monitor.Interval,
        )
        if s.callbacks.OnMonitorChange != nil {
            s.callbacks.OnMonitorChange(newCfg)
        }
    }

    // Pushover (api_token/user_key) -> rebuild Notifier
    if !reflect.DeepEqual(oldCfg.Pushover, newCfg.Pushover) {
        s.logger.Info("pushover config changed, triggering rebuild",
            "token_changed", oldCfg.Pushover.ApiToken != newCfg.Pushover.ApiToken,
        )
        if s.callbacks.OnPushoverChange != nil {
            s.callbacks.OnPushoverChange(newCfg)
        }
    }

    // SelfUpdate (github_owner/github_repo) -> rebuild SelfUpdater
    if !reflect.DeepEqual(oldCfg.SelfUpdate, newCfg.SelfUpdate) {
        s.logger.Info("self_update config changed, triggering rebuild",
            "old_owner", oldCfg.SelfUpdate.GithubOwner,
            "new_owner", newCfg.SelfUpdate.GithubOwner,
        )
        if s.callbacks.OnSelfUpdateChange != nil {
            s.callbacks.OnSelfUpdateChange(newCfg)
        }
    }

    // HealthCheck (interval) -> rebuild HealthMonitor
    if !reflect.DeepEqual(oldCfg.HealthCheck, newCfg.HealthCheck) {
        s.logger.Info("health_check config changed, triggering rebuild",
            "old_interval", oldCfg.HealthCheck.Interval,
            "new_interval", newCfg.HealthCheck.Interval,
        )
        if s.callbacks.OnHealthCheckChange != nil {
            s.callbacks.OnHealthCheckChange(newCfg)
        }
    }

    // API bearer_token -> update auth config
    if oldCfg.API.BearerToken != newCfg.API.BearerToken {
        s.logger.Info("api.bearer_token changed, triggering update")
        if s.callbacks.OnBearerTokenChange != nil {
            s.callbacks.OnBearerTokenChange(newCfg)
        }
    }

    // Instances (add/remove/modify) -> instance lifecycle management
    if !reflect.DeepEqual(oldCfg.Instances, newCfg.Instances) {
        s.logger.Info("instances config changed, triggering instance update",
            "old_count", len(oldCfg.Instances),
            "new_count", len(newCfg.Instances),
        )
        if s.callbacks.OnInstancesChange != nil {
            s.callbacks.OnInstancesChange(newCfg)
        }
    }
}

// StopWatch stops the config file watcher.
func StopWatch() {
    if globalHotReload == nil {
        return
    }
    globalHotReload.mu.Lock()
    defer globalHotReload.mu.Unlock()

    // viper.WatchConfig does not expose a Stop method.
    // The watcher goroutine stops when the viper instance is garbage collected
    // or the process exits. Set running=false to prevent callback execution.
    globalHotReload.running = false
    globalHotReload = nil
}

// GetCurrentConfig returns the most recently loaded config (may be hot-reloaded).
// Returns nil if WatchConfig has not been started.
func GetCurrentConfig() *Config {
    if globalHotReload == nil {
        return nil
    }
    globalHotReload.mu.Lock()
    defer globalHotReload.mu.Unlock()
    return globalHotReload.current
}
```

关键设计决策：
1. HotReloadCallbacks 使用函数回调而非直接引用组件（避免循环依赖，config 包不能 import network/notifier/health/instance/selfupdate 包）
2. reflect.DeepEqual 对比配置项，只重建真正变更的部分（D-07）
3. ReloadConfig 失败时保留旧配置继续运行（graceful degradation）
4. api.port 和 service 配置不在 handleConfigChange 中对比（D-06）
5. 全局 hotReloadState 保证线程安全（sync.Mutex）
  </action>
  <acceptance_criteria>
    - config.go 包含包级变量 `var viperInstance *viper.Viper`
    - config.go 包含函数 `func GetViper() *viper.Viper`
    - config.go 包含函数 `func ReloadConfig(old *Config) (*Config, error)`
    - config.go 的 Load 函数使用 `viperInstance` 而非局部变量 `v`
    - hotreload.go 包含 `type HotReloadCallbacks struct` 且有 6 个回调字段
    - hotreload.go 包含 `func WatchConfig(currentCfg *Config, logger *slog.Logger, callbacks *HotReloadCallbacks)`
    - hotreload.go 包含 `func StopWatch()`
    - hotreload.go 包含 `func GetCurrentConfig() *Config`
    - hotreload.go 的 handleConfigChange 对比 Monitor/Pushover/SelfUpdate/HealthCheck/BearerToken/Instances
    - hotreload.go 的 handleConfigChange 不对比 api.port 和 service
    - `go build ./internal/config/` 编译通过
    - `go vet ./internal/config/` 无警告
  </acceptance_criteria>
  <verify>
    <automated>cd /c/WorkSpace/agent/nanobot_auto_update && go build ./internal/config/ && go vet ./internal/config/</automated>
  </verify>
  <done>config.go 导出 viper 实例，hotreload.go 实现 WatchConfig/StopWatch/GetCurrentConfig 和基于回调的组件重建框架，编译通过</done>
</task>

<task type="auto">
  <name>Task 2: service_windows.go onReady 回调 + main.go 集成 WatchConfig (D-05/D-07)</name>
  <files>internal/lifecycle/service_windows.go, internal/lifecycle/service.go, cmd/nanobot-auto-updater/main.go</files>
  <read_first>
    - internal/lifecycle/service_windows.go (当前 ServiceHandler 和 RunService 签名)
    - internal/lifecycle/service.go (非 Windows RunService stub)
    - cmd/nanobot-auto-updater/main.go (当前 main.go 结构，第 270-281 行服务模式分支)
    - internal/config/hotreload.go (本 plan Task 1 创建的热重载模块)
    - internal/lifecycle/app.go (AppComponents 结构体)
    - internal/network/monitor.go (NetworkMonitor Stop/Start)
    - internal/notification/manager.go (NotificationManager Stop/Start)
    - internal/health/monitor.go (HealthMonitor Stop/Start)
    - internal/selfupdate/selfupdate.go (Updater NewUpdater)
    - internal/notifier/notifier.go (Notifier NewWithConfig)
    - internal/instance/manager.go (InstanceManager NewInstanceManager, StartAllInstances)
  </read_first>
  <action>
**采用方案：在 RunService/ServiceHandler 中添加 onReady 回调，在 AppStartup 成功后调用。** 这是最干净的方案，改动仅限于 service_windows.go、service.go（非 Windows stub）和 main.go。此方案被选择是因为 WatchConfig 需要访问 AppComponents（在 AppStartup 内创建），而 RunService 是阻塞的，必须在进入之前传入回调。

**Step 1: 修改 internal/lifecycle/service_windows.go**

1. 在 ServiceHandler struct 中添加 onReady 字段：
```go
type ServiceHandler struct {
    cfg              *config.Config
    logger           *slog.Logger
    version          string
    updateLogger     LogScheduler
    notif            NotifySender
    createComponents CreateComponentsFunc
    startInstances   StartInstancesFunc
    onReady          func(*AppComponents) // called after AppStartup succeeds
}
```

2. 更新 NewServiceHandler 签名，添加 onReady 参数：
```go
func NewServiceHandler(
    cfg *config.Config,
    logger *slog.Logger,
    version string,
    updateLogger LogScheduler,
    notif NotifySender,
    createComponents CreateComponentsFunc,
    startInstances StartInstancesFunc,
    onReady func(*AppComponents),
) *ServiceHandler {
    return &ServiceHandler{
        cfg:              cfg,
        logger:           logger,
        version:          version,
        updateLogger:     updateLogger,
        notif:            notif,
        createComponents: createComponents,
        startInstances:   startInstances,
        onReady:          onReady,
    }
}
```

3. 在 Execute 方法中，在报告 Running 状态之后、事件循环之前调用 onReady：
```go
// D-02: Report running state, accept Stop and Shutdown
s <- svc.Status{State: svc.Running, Accepts: cmdsAccepted}
h.logger.Info("Service is running")

// Call onReady callback after AppStartup succeeds (e.g., to start config hot reload)
if h.onReady != nil {
    h.onReady(components)
}
```

4. 更新 RunService 签名，添加 onReady 参数并传递给 NewServiceHandler：
```go
func RunService(
    cfg *config.Config,
    logger *slog.Logger,
    version string,
    updateLogger LogScheduler,
    notif NotifySender,
    createComponents CreateComponentsFunc,
    startInstances StartInstancesFunc,
    onReady func(*AppComponents),
) error {
    handler := NewServiceHandler(cfg, logger, version, updateLogger, notif, createComponents, startInstances, onReady)
    return svc.Run(cfg.Service.ServiceName, handler)
}
```

**Step 2: 修改 internal/lifecycle/service.go（非 Windows stub）**

更新 RunService 和 NewServiceHandler 的签名以匹配 Windows 版本，添加 `onReady func(*AppComponents)` 参数。stub 实现中忽略该参数：
```go
func NewServiceHandler(
    cfg *config.Config,
    logger *slog.Logger,
    version string,
    updateLogger LogScheduler,
    notif NotifySender,
    createComponents CreateComponentsFunc,
    startInstances StartInstancesFunc,
    onReady func(*AppComponents),
) *ServiceHandler {
    return &ServiceHandler{}
}

func RunService(
    cfg *config.Config,
    logger *slog.Logger,
    version string,
    updateLogger LogScheduler,
    notif NotifySender,
    createComponents CreateComponentsFunc,
    startInstances StartInstancesFunc,
    onReady func(*AppComponents),
) error {
    return fmt.Errorf("service mode is not supported on this platform")
}
```

**Step 3: 修改 cmd/nanobot-auto-updater/main.go**

在 main.go 的服务模式分支中，构建 HotReloadCallbacks 和 onReady 回调闭包，传入 RunService：

1. 在 RunService 调用之前，构建 HotReloadCallbacks（所有回调在 main.go 中定义，因为只有 main.go 可以导入所有组件包）。回调通过闭包捕获 components 引用（由 AppStartup 在 RunService 内部创建，通过 onReady 回调传出）：

```go
// 声明共享指针，onReady 会设置它，热重载回调会读取它
var hotReloadComponents *lifecycle.AppComponents

onReady := func(components *lifecycle.AppComponents) {
    hotReloadComponents = components

    hotReloadCallbacks := &config.HotReloadCallbacks{
        OnMonitorChange: func(newCfg *config.Config) {
            if hotReloadComponents.NetworkMonitor != nil {
                hotReloadComponents.NetworkMonitor.Stop()
            }
            if hotReloadComponents.NotificationManager != nil {
                hotReloadComponents.NotificationManager.Stop()
            }
            hotReloadComponents.NetworkMonitor = network.NewNetworkMonitor(
                "https://www.google.com",
                newCfg.Monitor.Interval,
                newCfg.Monitor.Timeout,
                logger,
            )
            go hotReloadComponents.NetworkMonitor.Start()
            hotReloadComponents.NotificationManager = notification.NewNotificationManager(
                hotReloadComponents.NetworkMonitor,
                notif,
                logger,
            )
            go hotReloadComponents.NotificationManager.Start(newCfg.Monitor.Interval)
        },

        OnPushoverChange: func(newCfg *config.Config) {
            notif = notifier.NewWithConfig(
                notifier.Config{
                    ApiToken: newCfg.Pushover.ApiToken,
                    UserKey:  newCfg.Pushover.UserKey,
                },
                logger,
            )
            if hotReloadComponents.NotificationManager != nil {
                hotReloadComponents.NotificationManager.Stop()
                hotReloadComponents.NotificationManager = notification.NewNotificationManager(
                    hotReloadComponents.NetworkMonitor,
                    notif,
                    logger,
                )
                go hotReloadComponents.NotificationManager.Start(newCfg.Monitor.Interval)
            }
        },

        OnSelfUpdateChange: func(newCfg *config.Config) {
            hotReloadComponents.SelfUpdater = selfupdate.NewUpdater(
                selfupdate.SelfUpdateConfig{
                    GithubOwner: newCfg.SelfUpdate.GithubOwner,
                    GithubRepo:  newCfg.SelfUpdate.GithubRepo,
                },
                logger,
            )
        },

        OnHealthCheckChange: func(newCfg *config.Config) {
            if hotReloadComponents.HealthMonitor != nil {
                hotReloadComponents.HealthMonitor.Stop()
            }
            im := hotReloadComponents.InstanceManager.(*instance.InstanceManager)
            hm := health.NewHealthMonitor(
                func() []health.InstanceStatus {
                    statuses := im.GetInstanceStatuses()
                    result := make([]health.InstanceStatus, len(statuses))
                    for i, s := range statuses {
                        result[i] = health.InstanceStatus{
                            Name:    s.Name,
                            Port:    s.Port,
                            Running: s.Running,
                            PID:     s.PID,
                        }
                    }
                    return result
                },
                newCfg.HealthCheck.Interval,
                logger,
            )
            hotReloadComponents.HealthMonitor = hm
            go hm.Start()
        },

        OnBearerTokenChange: func(newCfg *config.Config) {
            slog.Info("bearer token updated via hot reload")
        },

        OnInstancesChange: func(newCfg *config.Config) {
            im := hotReloadComponents.InstanceManager.(*instance.InstanceManager)
            slog.Info("instances config changed via hot reload",
                "old_count", len(im.GetInstanceNames()),
                "new_count", len(newCfg.Instances),
            )
            hotReloadComponents.InstanceManager = instance.NewInstanceManager(newCfg, logger, notif)
            ctx, cancel := context.WithTimeout(context.Background(), 5*time.Minute)
            defer cancel()
            newIM := hotReloadComponents.InstanceManager.(*instance.InstanceManager)
            newIM.StartAllInstances(ctx)
        },
    }

    config.WatchConfig(cfg, logger, hotReloadCallbacks)
}
```

2. 将 `onReady` 传入 RunService 调用：
```go
err = lifecycle.RunService(cfg, logger, version, updateLogger, notif, createComponents, startInstances, onReady)
```

3. 确保 main.go 的 import 包含 `"context"`, `"time"`, `"github.com/HQGroup/nanobot-auto-updater/internal/config"` 和所有组件包（network, notification, health, notifier, instance, selfupdate）。注意这些 import 可能已存在，只添加缺失的。
  </action>
  <acceptance_criteria>
    - service_windows.go 的 ServiceHandler 有 `onReady func(*AppComponents)` 字段
    - service_windows.go 的 Execute 方法在报告 Running 状态后调用 `h.onReady(components)`（如果非 nil）
    - RunService（service_windows.go）签名包含 `onReady func(*AppComponents)` 参数
    - service.go（非 Windows stub）的 RunService 和 NewServiceHandler 签名同步更新
    - main.go 服务模式分支中定义了 onReady 回调闭包
    - main.go 的 onReady 内部构建 HotReloadCallbacks 并调用 config.WatchConfig
    - main.go 的 RunService 调用传入 onReady 参数
    - `go build ./cmd/nanobot-auto-updater/` 编译通过
    - `go vet ./cmd/nanobot-auto-updater/` 无警告
  </acceptance_criteria>
  <verify>
    <automated>cd /c/WorkSpace/agent/nanobot_auto_update && go build ./cmd/nanobot-auto-updater/ && go vet ./cmd/nanobot-auto-updater/</automated>
  </verify>
  <done>service_windows.go 添加 onReady 回调（AppStartup 成功后调用），main.go 通过 onReady 启动 config.WatchConfig 并注册 6 个组件重建回调。service.go 非 Windows stub 签名同步更新。api.port 和 service 配置不热重载。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml -> viper.WatchConfig | 文件系统变更触发组件重建 |
| OnConfigChange callback -> components | 配置变更导致运行时组件替换 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | Tampering | config.yaml hot reload | mitigate | ReloadConfig 在回调中执行 Validate() 验证新配置合法性。验证失败保留旧配置。 |
| T-49-04 | Denial of Service | rapid config changes | accept | viper.WatchConfig 使用 fsnotify，操作系统级去抖。配置重建是轻量操作。 |
| T-49-05 | Information Disclosure | bearer_token in logs | mitigate | 日志中不输出 bearer_token 值，仅记录 "bearer_token changed" 布尔值 |
| T-49-06 | Elevation of Privilege | instance hot reload | accept | 实例管理器以当前用户权限运行，热重载不改变权限级别 |
</threat_model>

<verification>
1. `go build ./cmd/nanobot-auto-updater/` 编译通过
2. `go vet ./...` 无警告
3. hotreload.go 包含 WatchConfig 函数
4. hotreload.go 包含 HotReloadCallbacks 结构体（6 个回调字段）
5. hotreload.go 包含 StopWatch 函数
6. hotreload.go 不对比 api.port 和 service 配置
7. main.go 包含 config.WatchConfig 调用（在 onReady 闭包内）
8. service_windows.go 包含 onReady 回调机制（ServiceHandler 字段 + Execute 调用）
9. service.go 非 Windows stub 签名与 service_windows.go 匹配
</verification>

<success_criteria>
- 服务模式下 config.yaml 文件变更自动触发组件重建
- 仅重建受影响的组件（D-07）
- api.port 和 service 配置不热重载（D-06）
- 配置重载失败时保留旧配置继续运行
- 控制台模式下不启动 WatchConfig
- 所有编译和静态检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/49-existing-code-adaptation/49-02-SUMMARY.md`
</output>
