---
phase: 21-instance-management-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [internal/logbuffer/buffer.go, internal/logbuffer/buffer_test.go]
autonomous: true
requirements: [INST-05]
user_setup: []
must_haves:
  truths:
    - "LogBuffer.Clear() resets buffer size to 0"
    - "LogBuffer.Clear() resets head position to 0"
    - "GetHistory() returns empty slice after Clear()"
    - "Subscribers continue to receive logs after Clear()"
  artifacts:
    - path: "internal/logbuffer/buffer.go"
      provides: "Clear() method for INST-05 support"
      contains: "func (lb *LogBuffer) Clear()"
    - path: "internal/logbuffer/buffer_test.go"
      provides: "Test coverage for Clear method"
      contains: "TestLogBuffer_Clear"
  key_links:
    - from: "InstanceLifecycle.StartAfterUpdate"
      to: "LogBuffer.Clear"
      via: "method call before StartNanobotWithCapture"
      pattern: "logBuffer\\.Clear\\(\\)"
---

<objective>
Add Clear() method to LogBuffer to support instance restart behavior (INST-05).

Purpose: Instance restart requires fresh buffer - old logs should be discarded when instance restarts after update.
Output: Clear() method with test coverage.
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-instance-management-integration/21-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From internal/logbuffer/buffer.go:
```go
type LogBuffer struct {
    mu          sync.RWMutex
    entries     [5000]LogEntry
    head        int     // Next write position (0-4999)
    size        int     // Current entry count (0-5000)
    subscribers map[chan LogEntry]context.CancelFunc
    logger      *slog.Logger
}

type LogEntry struct {
    Timestamp time.Time
    Source    string    // "stdout" or "stderr"
    Content   string
}
```

Existing methods that Clear() should work with:
- `Write(entry LogEntry) error` - uses head, size, entries
- `GetHistory() []LogEntry` - reads head, size, entries
</interfaces>
</context>

<feature>
  <name>LogBuffer.Clear() method</name>
  <files>internal/logbuffer/buffer.go, internal/logbuffer/buffer_test.go</files>
  <behavior>
    - Clear() resets circular buffer state (head=0, size=0)
    - Clear() zeros out the entries array
    - GetHistory() returns empty slice after Clear()
    - Write() works correctly after Clear() (starts from position 0)
    - Subscribers continue to receive new logs after Clear()
    Cases:
      - Empty buffer: Clear() is no-op, size stays 0
      - Buffer with entries: Clear() resets to empty state
      - Buffer after Clear(): Write() works from position 0
  </behavior>
  <implementation>
    Add Clear() method to LogBuffer with:
    1. Lock mutex (lb.mu.Lock())
    2. Reset head = 0
    3. Reset size = 0
    4. Zero entries array: lb.entries = [5000]LogEntry{}
    5. Log debug message
    6. Note in comment: subscribers continue receiving new logs after clear

    Test cases in buffer_test.go:
    1. TestLogBuffer_Clear - basic Clear() functionality
    2. TestLogBuffer_Clear_EmptyBuffer - Clear() on empty buffer
    3. TestLogBuffer_Clear_WriteAfterClear - Write() works after Clear()
  </implementation>
</feature>

<verification>
Run: `go test ./internal/logbuffer -run TestLogBuffer_Clear -v`
All 3 Clear tests must pass.
</verification>

<success_criteria>
- LogBuffer.Clear() method exists and compiles
- TestLogBuffer_Clear passes
- TestLogBuffer_Clear_EmptyBuffer passes
- TestLogBuffer_Clear_WriteAfterClear passes
- `go test ./internal/logbuffer -v` passes all tests
</success_criteria>

<output>
After completion, create `.planning/phases/21-instance-management-integration/21-01-SUMMARY.md`
</output>
