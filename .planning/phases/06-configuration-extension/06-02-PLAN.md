---
phase: 06-configuration-extension
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - internal/config/instance_test.go
  - internal/config/config_test.go
  - testutil/testdata/config/instances_valid.yaml
  - testutil/testdata/config/instances_duplicate_name.yaml
  - testutil/testdata/config/instances_duplicate_port.yaml
  - testutil/testdata/config/legacy_v1.yaml
  - testutil/testdata/config/mixed_mode.yaml
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
must_haves:
  truths:
    - "用户可以加载包含多个实例的 YAML 配置文件"
    - "重复名称被检测并报告详细的错误消息"
    - "重复端口被检测并报告详细的错误消息"
    - "旧配置文件正常加载并使用单实例模式"
    - "混合模式配置被检测并报错"
  artifacts:
    - path: "internal/config/instance_test.go"
      provides: "InstanceConfig 单元测试"
      min_lines: 80
      contains: "TestInstanceConfig"
    - path: "internal/config/config_test.go"
      provides: "Config 集成测试和多实例验证测试"
      contains: "TestLoadInstancesYAML, TestValidateUniqueNames, TestValidateModeCompatibility"
      min_lines: 150
    - path: "testutil/testdata/config/*.yaml"
      provides: "各种配置场景的测试数据"
      count: 5
  key_links:
    - from: "testutil/testdata/config/instances_valid.yaml"
      to: "config.Load"
      via: "测试加载"
      pattern: "Load\\(.*instances_valid\\.yaml"
    - from: "TestValidateUniqueNames"
      to: "validateUniqueNames"
      via: "单元测试"
      pattern: "validateUniqueNames\\(instances\\)"
    - from: "TestValidateModeCompatibility"
      to: "ValidateModeCompatibility"
      via: "集成测试"
      pattern: "ValidateModeCompatibility\\(\\)"
---

<objective>
创建测试用例和测试数据验证多实例配置

Purpose: 通过完整的测试覆盖确保多实例配置的正确性,验证边界条件、错误消息和向后兼容性
Output: 完整的测试套件和测试数据文件
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-configuration-extension/06-CONTEXT.md
@.planning/phases/06-configuration-extension/06-RESEARCH.md
@.planning/phases/06-configuration-extension/06-01-PLAN.md

<interfaces>
<!-- 来自 06-01 计划创建的接口 -->

```go
// InstanceConfig 结构 (来自 06-01)
type InstanceConfig struct {
    Name           string        `mapstructure:"name"`
    Port           uint32        `mapstructure:"port"`
    StartCommand   string        `mapstructure:"start_command"`
    StartupTimeout time.Duration `mapstructure:"startup_timeout"`
}

// Config 扩展 (来自 06-01)
type Config struct {
    Cron      string
    Nanobot   NanobotConfig
    Instances []InstanceConfig  // 新增
    Pushover  PushoverConfig
}

// 验证函数 (来自 06-01)
func (ic *InstanceConfig) Validate() error
func (c *Config) ValidateModeCompatibility() error
func validateUniqueNames(instances []InstanceConfig) error
func validateUniquePorts(instances []InstanceConfig) error
func (c *Config) Validate() error
func Load(configPath string) (*Config, error)
```

<!-- 测试策略 - 来自 RESEARCH.md -->
- 使用 table-driven tests 模式
- 验证错误消息内容 (不仅仅是错误存在)
- 测试边界条件 (空值、最小值、最大值)
- 测试向后兼容性 (旧配置仍可加载)
- 使用 errors.Is/As 检查错误链
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 InstanceConfig 和多实例验证单元测试</name>
  <files>internal/config/instance_test.go, internal/config/config_test.go</files>
  <behavior>
    - Test 1: TestInstanceConfig_Validate_Valid - 验证有效的 InstanceConfig 通过验证
    - Test 2: TestInstanceConfig_Validate_EmptyName - 验证空 name 字段返回错误
    - Test 3: TestInstanceConfig_Validate_InvalidPort - 验证无效 port 返回错误
    - Test 4: TestInstanceConfig_Validate_EmptyStartCommand - 验证空 start_command 返回错误
    - Test 5: TestInstanceConfig_Validate_InvalidStartupTimeout - 验证 startup_timeout < 5s 返回错误
    - Test 6: TestValidateUniqueNames_Duplicate - 验证重复名称被检测
    - Test 7: TestValidateUniqueNames_Valid - 验证唯一名称通过
    - Test 8: TestValidateUniqueNames_ErrorMessage - 验证错误消息包含位置信息
    - Test 9: TestValidateUniquePorts_Duplicate - 验证重复端口被检测
    - Test 10: TestValidateUniquePorts_Valid - 验证唯一端口通过
    - Test 11: TestValidateUniquePorts_ErrorMessage - 验证错误消息包含实例名称
    - Test 12: TestConfig_ValidateModeCompatibility_Mixed - 验证混合模式被检测
    - Test 13: TestConfig_ValidateModeCompatibility_Legacy - 验证旧模式通过
    - Test 14: TestConfig_ValidateModeCompatibility_Instances - 验证新模式通过
  </behavior>
  <action>
创建和扩展测试文件:

**第一部分: 创建 internal/config/instance_test.go**

1. **TestInstanceConfig_Validate** (table-driven test):
   ```go
   func TestInstanceConfig_Validate(t *testing.T) {
       tests := []struct {
           name    string
           config  InstanceConfig
           wantErr bool
           errMsg  string // 期望的错误消息片段
       }{
           {
               name: "valid config",
               config: InstanceConfig{
                   Name: "instance1",
                   Port: 18790,
                   StartCommand: "nanobot.exe",
                   StartupTimeout: 30 * time.Second,
               },
               wantErr: false,
           },
           {
               name: "empty name",
               config: InstanceConfig{
                   Name: "",
                   Port: 18790,
                   StartCommand: "nanobot.exe",
               },
               wantErr: true,
               errMsg: "name",
           },
           // ... 更多测试用例
       }
       // ... 测试逻辑
   }
   ```

2. **TestValidateUniqueNames** (table-driven test):
   - 测试用例: 重复名称 (第2和第5个实例)
   - 测试用例: 唯一名称通过
   - 验证错误消息包含 "第 2 和第 5"

3. **TestValidateUniquePorts** (table-driven test):
   - 测试用例: 重复端口 (instance1 和 instance2)
   - 测试用例: 唯一端口通过
   - 验证错误消息包含实例名称

**第二部分: 扩展 internal/config/config_test.go**

4. **TestConfig_ValidateModeCompatibility** (table-driven test):
   ```go
   func TestConfig_ValidateModeCompatibility(t *testing.T) {
       tests := []struct {
           name    string
           config  Config
           wantErr bool
       }{
           {
               name: "mixed mode - both present",
               config: Config{
                   Nanobot: NanobotConfig{Port: 18790},
                   Instances: []InstanceConfig{{Name: "i1", Port: 18791, StartCommand: "cmd"}},
               },
               wantErr: true,
           },
           {
               name: "legacy mode only",
               config: Config{
                   Nanobot: NanobotConfig{Port: 18790},
               },
               wantErr: false,
           },
           {
               name: "instances mode only",
               config: Config{
                   Instances: []InstanceConfig{{Name: "i1", Port: 18791, StartCommand: "cmd"}},
               },
               wantErr: false,
           },
       }
       // ... 测试逻辑
   }
   ```

5. **TestConfig_Validate_MultipleErrors** (验证 errors.Join 聚合):
   - 创建有多个错误的配置 (重复名称 + 重复端口)
   - 验证返回的 error 包含所有错误信息

**测试最佳实践**:
- 使用 t.Helper() 标记辅助函数
- 使用 strings.Contains 检查错误消息
- 测试边界条件 (port=0, port=65536, timeout=4s)
- 验证错误消息的用户友好性 (包含实例名称、位置)

**避免的做法**:
- 不要只测试错误存在,要验证错误消息内容
- 不要使用硬编码的索引 (使用描述性的测试用例名称)
- 不要跳过边界条件测试
</action>
  <verify>
    <automated>go test ./internal/config -v -run "TestInstanceConfig|TestValidateUnique|TestConfig_ValidateMode"</automated>
  </verify>
  <done>所有单元测试创建完成并通过,测试覆盖所有边界条件和错误消息格式,使用 table-driven 模式</done>
</task>

<task type="auto">
  <name>Task 2: 创建测试数据文件和集成测试</name>
  <files>
    testutil/testdata/config/instances_valid.yaml,
    testutil/testdata/config/instances_duplicate_name.yaml,
    testutil/testdata/config/instances_duplicate_port.yaml,
    testutil/testdata/config/legacy_v1.yaml,
    testutil/testdata/config/mixed_mode.yaml,
    internal/config/config_test.go
  </files>
  <action>
创建测试数据文件和集成测试:

**第一部分: 创建测试数据 YAML 文件**

1. **testutil/testdata/config/instances_valid.yaml** (有效的多实例配置):
   ```yaml
   cron: "0 3 * * *"

   instances:
     - name: "instance1"
       port: 18790
       start_command: "C:\\path\\to\\nanobot.exe"
       startup_timeout: 30s

     - name: "instance2"
       port: 18791
       start_command: "C:\\another\\path\\nanobot.exe --port 18791"

   pushover:
     api_token: "test_token"
     user_key: "test_user"
   ```

2. **testutil/testdata/config/instances_duplicate_name.yaml** (重复名称):
   ```yaml
   cron: "0 3 * * *"

   instances:
     - name: "duplicate_name"
       port: 18790
       start_command: "cmd1"

     - name: "duplicate_name"
       port: 18791
       start_command: "cmd2"
   ```

3. **testutil/testdata/config/instances_duplicate_port.yaml** (重复端口):
   ```yaml
   cron: "0 3 * * *"

   instances:
     - name: "instance1"
       port: 18790
       start_command: "cmd1"

     - name: "instance2"
       port: 18790
       start_command: "cmd2"
   ```

4. **testutil/testdata/config/legacy_v1.yaml** (v1.0 单实例配置):
   ```yaml
   cron: "0 3 * * *"

   nanobot:
     port: 18790
     startup_timeout: 30s
     repo_path: "C:\\Users\\test\\.nanobot\\repo"

   pushover:
     api_token: "test_token"
     user_key: "test_user"
   ```

5. **testutil/testdata/config/mixed_mode.yaml** (混合模式 - 应该失败):
   ```yaml
   cron: "0 3 * * *"

   nanobot:
     port: 18790

   instances:
     - name: "instance1"
       port: 18791
       start_command: "cmd"
   ```

**第二部分: 扩展 internal/config/config_test.go 添加集成测试**

6. **TestLoadInstancesYAML** (端到端加载测试):
   ```go
   func TestLoadInstancesYAML(t *testing.T) {
       cfg, err := Load("../testutil/testdata/config/instances_valid.yaml")
       if err != nil {
           t.Fatalf("Load failed: %v", err)
       }

       // 验证加载的数据
       if len(cfg.Instances) != 2 {
           t.Errorf("expected 2 instances, got %d", len(cfg.Instances))
       }

       // 验证第一个实例
       if cfg.Instances[0].Name != "instance1" {
           t.Errorf("expected instance1, got %s", cfg.Instances[0].Name)
       }
       if cfg.Instances[0].Port != 18790 {
           t.Errorf("expected port 18790, got %d", cfg.Instances[0].Port)
       }

       // 验证第二个实例
       if cfg.Instances[1].Name != "instance2" {
           t.Errorf("expected instance2, got %s", cfg.Instances[1].Name)
       }
   }
   ```

7. **TestLoadLegacyConfig** (向后兼容性测试):
   ```go
   func TestLoadLegacyConfig(t *testing.T) {
       cfg, err := Load("../testutil/testdata/config/legacy_v1.yaml")
       if err != nil {
           t.Fatalf("Load legacy config failed: %v", err)
       }

       // 验证旧配置仍然工作
       if cfg.Nanobot.Port != 18790 {
           t.Errorf("expected port 18790, got %d", cfg.Nanobot.Port)
       }
       if cfg.Nanobot.StartupTimeout != 30*time.Second {
           t.Errorf("expected 30s timeout, got %v", cfg.Nanobot.StartupTimeout)
       }

       // 验证没有加载 instances
       if len(cfg.Instances) != 0 {
           t.Errorf("expected no instances, got %d", len(cfg.Instances))
       }
   }
   ```

8. **TestLoadDuplicateName** (重复名称验证):
   ```go
   func TestLoadDuplicateName(t *testing.T) {
       _, err := Load("../testutil/testdata/config/instances_duplicate_name.yaml")
       if err == nil {
           t.Fatal("expected error for duplicate name, got nil")
       }

       // 验证错误消息包含 "实例名称重复"
       if !strings.Contains(err.Error(), "实例名称重复") {
           t.Errorf("error message should contain '实例名称重复', got: %v", err)
       }
   }
   ```

9. **TestLoadDuplicatePort** (重复端口验证):
   ```go
   func TestLoadDuplicatePort(t *testing.T) {
       _, err := Load("../testutil/testdata/config/instances_duplicate_port.yaml")
       if err == nil {
           t.Fatal("expected error for duplicate port, got nil")
       }

       // 验证错误消息包含 "端口重复"
       if !strings.Contains(err.Error(), "端口重复") {
           t.Errorf("error message should contain '端口重复', got: %v", err)
       }
   }
   ```

10. **TestLoadMixedMode** (混合模式验证):
    ```go
    func TestLoadMixedMode(t *testing.T) {
        _, err := Load("../testutil/testdata/config/mixed_mode.yaml")
        if err == nil {
            t.Fatal("expected error for mixed mode, got nil")
        }

        // 验证错误消息包含 "不能同时使用"
        if !strings.Contains(err.Error(), "不能同时使用") {
            t.Errorf("error message should contain '不能同时使用', got: %v", err)
        }
    }
    ```

**测试数据准备注意事项**:
- 使用真实的 YAML 语法 (不要使用占位符)
- 确保路径使用 Windows 风格 (反斜杠或正斜杠都可以)
- startup_timeout 使用 Go duration 字符串格式 (30s, 1m, etc.)
- 包含 cron 和 pushover section 确保完整配置

**避免的做法**:
- 不要在测试中使用相对路径而不检查工作目录
- 不要跳过错误消息内容验证
- 不要创建无效的 YAML 语法 (会导致解析错误而非验证错误)
</action>
  <verify>
    <automated>go test ./internal/config -v -run "TestLoad"</automated>
  </verify>
  <done>所有测试数据文件创建完成,集成测试通过,端到端加载和验证流程工作正常,向后兼容性验证通过</done>
</task>

</tasks>

<verification>
## Phase Level Verification

完成此计划后,Phase 6 的所有测试应该就绪:

1. **所有测试通过**:
   ```bash
   go test ./internal/config -v
   ```

2. **测试覆盖率**:
   ```bash
   go test ./internal/config -cover
   ```
   目标: 覆盖率 > 80%

3. **测试数据完整性**:
   - 5 个 YAML 测试文件存在
   - 每个文件代表一个配置场景
   - 文件内容符合 YAML 语法

4. **集成测试验证**:
   - 有效的多实例配置加载成功
   - 旧配置文件向后兼容
   - 重复名称/端口被检测
   - 混合模式被拒绝

5. **单元测试验证**:
   - InstanceConfig 验证覆盖所有字段
   - 唯一性检查算法正确
   - 错误消息格式符合要求
   - errors.Join 聚合工作正常
</verification>

<success_criteria>
## Measurable Completion

- [ ] internal/config/instance_test.go 创建完成
- [ ] internal/config/config_test.go 扩展完成
- [ ] testutil/testdata/config/instances_valid.yaml 创建
- [ ] testutil/testdata/config/instances_duplicate_name.yaml 创建
- [ ] testutil/testdata/config/instances_duplicate_port.yaml 创建
- [ ] testutil/testdata/config/legacy_v1.yaml 创建
- [ ] testutil/testdata/config/mixed_mode.yaml 创建
- [ ] 所有单元测试通过 (TestInstanceConfig*, TestValidateUnique*, TestConfig_ValidateMode*)
- [ ] 所有集成测试通过 (TestLoad*)
- [ ] 测试覆盖率 > 80%
- [ ] 错误消息格式验证通过 (包含实例名称、位置信息)
- [ ] 向后兼容性验证通过 (legacy_v1.yaml 加载成功)
</success_criteria>

<output>
After completion, create `.planning/phases/06-configuration-extension/06-02-SUMMARY.md`
</output>
