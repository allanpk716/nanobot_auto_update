---
phase: 26-network-monitoring-core
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - cmd/nanobot-auto-updater/main.go
autonomous: true
requirements: [MONITOR-01, MONITOR-06]

must_haves:
  truths:
    - "应用启动后网络监控自动开始运行"
    - "网络监控在健康监控之后启动"
    - "应用关闭时网络监控先于健康监控停止"
    - "监控间隔和超时通过配置文件 config.yaml 调整"
  artifacts:
    - path: "cmd/nanobot-auto-updater/main.go"
      provides: "NetworkMonitor 生命周期集成"
      contains: "network.NewNetworkMonitor"
  key_links:
    - from: "main.go"
      to: "internal/network/monitor.go"
      via: "network.NewNetworkMonitor, Start(), Stop()"
      pattern: "networkMonitor\\.(Start|Stop)"
    - from: "main.go"
      to: "config.yaml"
      via: "cfg.Monitor.Interval, cfg.Monitor.Timeout"
      pattern: "cfg\\.Monitor\\."
---

<objective>
将 NetworkMonitor 集成到 main.go 应用程序生命周期，在健康监控之后启动网络监控，在关闭时优先停止网络监控。

**Purpose:** 实现 NetworkMonitor 生命周期管理，满足 MONITOR-01, MONITOR-06 需求
**Output:** 修改 cmd/nanobot-auto-updater/main.go
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-network-monitoring-core/26-CONTEXT.md

## Reference Implementation (Phase 25 HealthMonitor Integration)

From cmd/nanobot-auto-updater/main.go:
```go
// Start health monitor for all instances (HEALTH-01, HEALTH-04)
var healthMonitor *health.HealthMonitor
if len(cfg.Instances) > 0 {
    healthMonitor = health.NewHealthMonitor(
        cfg.Instances,
        cfg.HealthCheck.Interval,
        logger,
    )
    go healthMonitor.Start()
    logger.Info("健康监控已启动", "interval", cfg.HealthCheck.Interval)
}

// ... graceful shutdown ...
// Stop health monitor first
if healthMonitor != nil {
    healthMonitor.Stop()
}
// Shutdown API server
if apiServer != nil {
    apiServer.Shutdown(shutdownCtx)
}
```

## Startup Order (from CONTEXT.md)
启动顺序: 配置加载 → InstanceManager 创建 → API 服务器启动 → 健康监控启动 → 网络监控启动

## Shutdown Order (from CONTEXT.md)
关闭顺序: 网络监控先停 → 健康监控后停 → API 服务器最后停

## Configuration Available
- cfg.Monitor.Interval (default: 15m)
- cfg.Monitor.Timeout (default: 10s)
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From internal/network/monitor.go (Plan 26-01 creates this):
```go
package network

type ConnectivityState struct {
    IsConnected bool
    LastCheck   time.Time
}

type NetworkMonitor struct { ... }

func NewNetworkMonitor(
    targetURL string,
    interval time.Duration,
    timeout time.Duration,
    logger *slog.Logger,
) *NetworkMonitor

func (nm *NetworkMonitor) Start()  // 在独立 goroutine 中运行
func (nm *NetworkMonitor) Stop()   // 优雅停止
func (nm *NetworkMonitor) GetState() *ConnectivityState  // 获取当前状态
```

From internal/config/monitor.go:
```go
type MonitorConfig struct {
    Interval time.Duration `yaml:"interval"` // Default: 15m
    Timeout  time.Duration `yaml:"timeout"`  // Default: 10s
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 集成 NetworkMonitor 到 main.go 生命周期</name>
  <files>cmd/nanobot-auto-updater/main.go</files>
  <read_first>
    - cmd/nanobot-auto-updater/main.go (当前实现)
    - internal/network/monitor.go (Plan 26-01 创建，需要导入)
    - internal/config/monitor.go (配置结构)
  </read_first>
  <acceptance_criteria>
    - main.go 包含 `"github.com/HQGroup/nanobot-auto-updater/internal/network"` 导入
    - main.go 包含 `network.NewNetworkMonitor("https://www.google.com", cfg.Monitor.Interval, cfg.Monitor.Timeout, logger)` 调用
    - main.go 包含 `go networkMonitor.Start()` 调用
    - main.go 包含 `networkMonitor.Stop()` 调用
    - 网络监控在健康监控启动代码之后启动
    - 网络监控在健康监控停止之前停止
    - 存在中文日志 "网络监控已启动"
    - `go build ./cmd/nanobot-auto-updater` 成功
  </acceptance_criteria>
  <action>
    修改 cmd/nanobot-auto-updater/main.go，添加 NetworkMonitor 生命周期集成。

    ## 步骤 1: 添加 network 包导入

    在 import 块中添加:
    ```go
    "github.com/HQGroup/nanobot-auto-updater/internal/network"
    ```

    ## 步骤 2: 创建并启动网络监控器

    在健康监控启动代码之后（约第 124 行），添加:
    ```go
    // Start network monitor (MONITOR-01, MONITOR-06)
    networkMonitor := network.NewNetworkMonitor(
        "https://www.google.com",
        cfg.Monitor.Interval,
        cfg.Monitor.Timeout,
        logger,
    )
    go networkMonitor.Start()
    logger.Info("网络监控已启动", "interval", cfg.Monitor.Interval)
    ```

    注意:
    - 网络监控始终启动（不检查实例数量，因为监控 Google 不依赖实例）
    - 使用 `cfg.Monitor.Interval` 和 `cfg.Monitor.Timeout` 配置
    - 使用中文日志 "网络监控已启动"

    ## 步骤 3: 优雅关闭网络监控器

    在 shutdown 代码块中，在健康监控停止之前添加网络监控停止:
    ```go
    // Stop network monitor first
    if networkMonitor != nil {
        networkMonitor.Stop()
    }

    // Stop health monitor
    if healthMonitor != nil {
        healthMonitor.Stop()
    }

    // Shutdown API server
    if apiServer != nil {
        if err := apiServer.Shutdown(shutdownCtx); err != nil {
            logger.Error("API server shutdown error", "error", err)
        }
    }
    ```

    ## 验证

    运行 `go build ./cmd/nanobot-auto-updater` 确保编译成功。
  </action>
  <verify>
    <automated>go build ./cmd/nanobot-auto-updater && grep -c "networkMonitor" cmd/nanobot-auto-updater/main.go</automated>
  </verify>
  <done>
    - main.go 导入 internal/network 包
    - NetworkMonitor 在健康监控之后启动
    - 使用 cfg.Monitor.Interval 和 cfg.Monitor.Timeout 配置
    - 网络监控在健康监控之前停止
    - 中文日志 "网络监控已启动" 存在
    - go build ./cmd/nanobot-auto-updater 编译成功
  </done>
</task>

</tasks>

<verification>
## Phase-Level Verification

运行完整测试套件:
```bash
go test -v ./...
```

编译验证:
```bash
go build ./cmd/nanobot-auto-updater
```

手动验证步骤:
1. 修改 config.yaml 设置较短的监控间隔（如 1m）用于测试
2. 启动应用程序
3. 验证日志中出现 "网络监控已启动 interval=1m0s"
4. 等待第一次连通性检查，验证日志格式:
   - 成功: "Google 连通性检查成功 duration=XXX status_code=200"
   - 失败: "Google 连通性检查失败 duration=XXX error_type=XXX"
5. 按 Ctrl+C 关闭应用，验证 "网络监控已停止" 在健康监控停止之前
</verification>

<success_criteria>
- [x] MONITOR-01: 网络监控在应用启动后自动开始运行
- [x] MONITOR-06: 监控间隔和超时通过 config.yaml 的 monitor.interval 和 monitor.timeout 配置
- [x] 启动顺序: 健康监控启动 → 网络监控启动
- [x] 关闭顺序: 网络监控停止 → 健康监控停止 → API 服务器停止
- [x] 中文日志: "网络监控已启动"
</success_criteria>

<output>
After completion, create `.planning/phases/26-network-monitoring-core/26-02-SUMMARY.md`
</output>
