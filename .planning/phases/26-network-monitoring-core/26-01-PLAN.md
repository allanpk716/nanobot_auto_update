---
phase: 26-network-monitoring-core
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/network/monitor.go
  - internal/network/monitor_test.go
autonomous: true
requirements: [MONITOR-01, MONITOR-02, MONITOR-03]

must_haves:
  truths:
    - "系统定期向 google.com 发送 HTTP HEAD 请求测试连通性"
    - "HTTP 200 OK 响应被视为连通成功，记录 INFO 日志"
    - "非 200 响应或网络错误被视为失败，记录 ERROR 日志"
    - "日志包含响应时间(ms)和错误类型分类"
  artifacts:
    - path: "internal/network/monitor.go"
      provides: "NetworkMonitor 核心实现"
      exports: ["NetworkMonitor", "ConnectivityState", "NewNetworkMonitor", "Start", "Stop", "GetState"]
      contains: "func (nm *NetworkMonitor) checkConnectivity()"
    - path: "internal/network/monitor_test.go"
      provides: "NetworkMonitor 单元测试"
      contains: "func TestNetworkMonitor"
  key_links:
    - from: "internal/network/monitor.go"
      to: "https://www.google.com"
      via: "HTTP HEAD request"
      pattern: "http.MethodHead"
    - from: "internal/network/monitor.go"
      to: "internal/config/monitor.go"
      via: "MonitorConfig.Interval, MonitorConfig.Timeout"
      pattern: "time.Duration"
---

<objective>
创建 NetworkMonitor 核心实现，定期向 https://www.google.com 发送 HTTP HEAD 请求测试网络连通性，记录成功/失败日志并追踪状态变化。

**Purpose:** 实现网络连通性监控核心逻辑，满足 MONITOR-01, MONITOR-02, MONITOR-03 需求
**Output:** internal/network/monitor.go, internal/network/monitor_test.go
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-network-monitoring-core/26-CONTEXT.md
@.planning/phases/26-network-monitoring-core/26-RESEARCH.md

## Reference Implementation (HealthMonitor Pattern)

From internal/health/monitor.go:
```go
type InstanceHealthState struct {
    IsRunning bool
    LastCheck time.Time
}

type HealthMonitor struct {
    instances []config.InstanceConfig
    interval  time.Duration
    logger    *slog.Logger
    states    map[string]*InstanceHealthState
    mu        sync.RWMutex
    ctx       context.Context
    cancel    context.CancelFunc
}

func NewHealthMonitor(...) *HealthMonitor {
    ctx, cancel := context.WithCancel(context.Background())
    return &HealthMonitor{...}
}

func (hm *HealthMonitor) Start() {
    ticker := time.NewTicker(hm.interval)
    defer ticker.Stop()
    hm.checkAllInstances() // Immediate first check
    for {
        select {
        case <-hm.ctx.Done():
            return
        case <-ticker.C:
            hm.checkAllInstances()
        }
    }
}

func (hm *HealthMonitor) Stop() {
    if hm.cancel != nil {
        hm.cancel()
    }
}
```

## Configuration (Already Exists)

From internal/config/monitor.go:
```go
type MonitorConfig struct {
    Interval time.Duration `yaml:"interval"` // Default: 15m
    Timeout  time.Duration `yaml:"timeout"`  // Default: 10s
}

func (mc *MonitorConfig) Validate() error {
    if mc.Interval < 1*time.Minute {
        return fmt.Errorf("monitor.interval must be at least 1 minute, got %v", mc.Interval)
    }
    if mc.Timeout < 1*time.Second {
        return fmt.Errorf("monitor.timeout must be at least 1 second, got %v", mc.Timeout)
    }
    return nil
}
```

## Target URL and HTTP Client Configuration

From CONTEXT.md locked decisions:
- Target URL: `https://www.google.com`
- HTTP Method: HEAD (not GET)
- Success criteria: Only HTTP 200 OK
- Disable redirect following: `CheckRedirect` returns `http.ErrUseLastResponse`
- Timeout: Use config `monitor.timeout` (default 10s)
</context>

<feature>
  <name>NetworkMonitor - 网络连通性监控</name>
  <files>internal/network/monitor.go, internal/network/monitor_test.go</files>
  <behavior>
    - MONITOR-01: 定期向 https://www.google.com 发送 HTTP HEAD 请求
    - MONITOR-02: HTTP 非 200 或网络错误时记录 ERROR 日志，包含响应时间和错误类型
    - MONITOR-03: HTTP 200 OK 时记录 INFO 日志，包含响应时间和状态码
    - 状态追踪: 维护 ConnectivityState 记录上一次连通性状态
    - 错误分类: DNS 解析失败、连接超时、连接拒绝、TLS 错误、HTTP 非 200
    - 禁用重定向跟随: 严格测试 google.com 直接响应
  </behavior>
  <implementation>
    1. 创建 ConnectivityState 结构体 (IsConnected bool, LastCheck time.Time)
    2. 创建 NetworkMonitor 结构体 (targetURL, interval, timeout, logger, httpClient, state, ctx, cancel)
    3. 实现 NewNetworkMonitor() 构造函数，配置禁用重定向的 HTTP 客户端
    4. 实现 Start() 方法：time.Ticker + goroutine + context 模式，立即执行首次检查
    5. 实现 checkConnectivity() 方法：发送 HEAD 请求，记录响应时间，更新状态，记录日志
    6. 实现 performCheck() 方法：执行 HTTP 请求，返回 (isConnected, statusCode, errorMsg)
    7. 实现 classifyError() 方法：类型断言分类错误（DNS、超时、TLS、连接拒绝）
    8. 实现 Stop() 方法：调用 cancel() 取消 context
    9. 实现 GetState() 方法：返回当前状态（供 Phase 27 使用）
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 NetworkMonitor 核心实现和测试</name>
  <files>internal/network/monitor.go, internal/network/monitor_test.go</files>
  <read_first>
    - internal/health/monitor.go (参考实现模式)
    - internal/config/monitor.go (配置结构)
    - .planning/phases/26-network-monitoring-core/26-RESEARCH.md (技术细节和错误分类)
  </read_first>
  <behavior>
    - Test 1: TestNewNetworkMonitor - 验证构造函数创建正确的 NetworkMonitor
    - Test 2: TestCheckConnectivity_Success - HTTP 200 返回 true, 记录 INFO 日志
    - Test 3: TestCheckConnectivity_Failure_Non200 - HTTP 非 200 返回 false, 记录 ERROR 日志
    - Test 4: TestCheckConnectivity_Failure_Timeout - 请求超时返回 false, 错误类型为"连接超时"
    - Test 5: TestCheckConnectivity_Failure_DNS - DNS 解析失败返回 false, 错误类型包含"DNS"
    - Test 6: TestClassifyError - 验证错误类型分类正确
    - Test 7: TestStateTracking - 验证状态追踪和变化检测
    - Test 8: TestGracefulStop - 验证 Stop() 能正确停止监控循环
    - Test 9: TestDisableRedirect - 验证禁用重定向跟随，301/302 不跟随
  </behavior>
  <action>
    创建 `internal/network/` 目录并实现 NetworkMonitor。

    ## 文件 1: internal/network/monitor.go

    ```go
    package network

    import (
        "context"
        "crypto/tls"
        "crypto/x509"
        "fmt"
        "log/slog"
        "net"
        "net/http"
        "net/url"
        "os"
        "syscall"
        "time"
    )

    // ConnectivityState 连通性状态
    type ConnectivityState struct {
        IsConnected bool
        LastCheck   time.Time
    }

    // NetworkMonitor 网络连通性监控器
    type NetworkMonitor struct {
        targetURL  string
        interval   time.Duration
        timeout    time.Duration
        logger     *slog.Logger
        httpClient *http.Client
        state      *ConnectivityState
        ctx        context.Context
        cancel     context.CancelFunc
    }

    // NewNetworkMonitor 创建网络监控器
    func NewNetworkMonitor(
        targetURL string,
        interval time.Duration,
        timeout time.Duration,
        logger *slog.Logger,
    ) *NetworkMonitor {
        ctx, cancel := context.WithCancel(context.Background())

        // 创建 HTTP 客户端，禁用重定向跟随
        client := &http.Client{
            Timeout: timeout,
            CheckRedirect: func(req *http.Request, via []*http.Request) error {
                return http.ErrUseLastResponse // 不跟随重定向
            },
        }

        return &NetworkMonitor{
            targetURL:  targetURL,
            interval:   interval,
            timeout:    timeout,
            logger:     logger.With("component", "network-monitor"),
            httpClient: client,
            state:      nil, // 初始状态为 nil，表示首次检查
            ctx:        ctx,
            cancel:     cancel,
        }
    }

    // Start 启动网络监控循环（在独立 goroutine 中运行）
    func (nm *NetworkMonitor) Start() {
        nm.logger.Info("网络监控已启动",
            "target", nm.targetURL,
            "interval", nm.interval,
            "timeout", nm.timeout)

        ticker := time.NewTicker(nm.interval)
        defer ticker.Stop()

        // 立即执行一次初始检查
        nm.checkConnectivity()

        for {
            select {
            case <-nm.ctx.Done():
                nm.logger.Info("网络监控已停止")
                return
            case <-ticker.C:
                nm.checkConnectivity()
            }
        }
    }

    // checkConnectivity 检查连通性并记录日志
    func (nm *NetworkMonitor) checkConnectivity() {
        start := time.Now()
        isConnected, statusCode, errMsg := nm.performCheck()
        duration := time.Since(start)

        // 更新状态
        now := time.Now()
        previousState := nm.state
        nm.state = &ConnectivityState{
            IsConnected: isConnected,
            LastCheck:   now,
        }

        // 记录日志
        if isConnected {
            // MONITOR-03: 成功时记录 INFO
            nm.logger.Info("Google 连通性检查成功",
                "duration", duration.Milliseconds(),
                "status_code", statusCode)
        } else {
            // MONITOR-02: 失败时记录 ERROR
            nm.logger.Error("Google 连通性检查失败",
                "duration", duration.Milliseconds(),
                "error_type", errMsg)
        }

        // 记录状态变化（首次检查、状态保持、状态改变）
        if previousState == nil {
            nm.logger.Info("初始连通性状态", "is_connected", isConnected)
        } else if previousState.IsConnected != isConnected {
            if previousState.IsConnected && !isConnected {
                nm.logger.Warn("连通性状态改变: 从连通变为不连通")
            } else {
                nm.logger.Info("连通性状态改变: 从不连通变为连通")
            }
        }
    }

    // performCheck 执行 HTTP HEAD 请求检查连通性
    // 返回: (是否连通, HTTP 状态码, 错误消息)
    func (nm *NetworkMonitor) performCheck() (bool, int, string) {
        req, err := http.NewRequest(http.MethodHead, nm.targetURL, nil)
        if err != nil {
            return false, 0, fmt.Sprintf("创建请求失败: %v", err)
        }

        resp, err := nm.httpClient.Do(req)
        if err != nil {
            errMsg := nm.classifyError(err)
            return false, 0, errMsg
        }
        defer resp.Body.Close()

        // 仅 HTTP 200 OK 算成功
        if resp.StatusCode == http.StatusOK {
            return true, resp.StatusCode, ""
        }

        // 非 200 状态码视为失败
        errMsg := fmt.Sprintf("HTTP 状态码 %d (%s)", resp.StatusCode, resp.Status)
        return false, resp.StatusCode, errMsg
    }

    // classifyError 分类错误类型
    func (nm *NetworkMonitor) classifyError(err error) string {
        // 解析 URL 错误
        if urlErr, ok := err.(*url.Error); ok {
            // 超时错误
            if urlErr.Timeout() {
                return "连接超时"
            }

            innerErr := urlErr.Err

            // DNS 解析失败
            if netErr, ok := innerErr.(*net.DNSError); ok {
                return fmt.Sprintf("DNS 解析失败: %s", netErr.Host)
            }

            // 连接拒绝
            if netErr, ok := innerErr.(*net.OpError); ok {
                if syscallErr, ok := netErr.Err.(*os.SyscallError); ok {
                    if syscallErr.Err == syscall.ECONNREFUSED {
                        return "连接被拒绝"
                    }
                }
            }

            // TLS 握手错误
            if _, ok := innerErr.(tls.CertificateVerificationError); ok {
                return "TLS 证书验证失败"
            }
            if _, ok := innerErr.(x509.UnknownAuthorityError); ok {
                return "TLS 未知证书颁发机构"
            }

            return fmt.Sprintf("网络错误: %v", innerErr)
        }

        return fmt.Sprintf("未知错误: %v", err)
    }

    // Stop 停止网络监控
    func (nm *NetworkMonitor) Stop() {
        nm.logger.Info("正在停止网络监控...")
        nm.cancel()
    }

    // GetState 获取当前连通性状态（供 Phase 27 使用）
    func (nm *NetworkMonitor) GetState() *ConnectivityState {
        return nm.state
    }
    ```

    ## 文件 2: internal/network/monitor_test.go

    创建测试文件，使用 httptest 模拟 HTTP 服务器测试各种场景：
    - 成功场景: 服务器返回 200
    - 非 200 场景: 服务器返回 301/302/404/500
    - 超时场景: 服务器延迟响应
    - 重定向场景: 服务器返回 301/302，验证不跟随
    - 状态追踪: 验证状态变化检测

    注意事项:
    - 使用中文日志消息（符合项目规范）
    - 响应时间单位为毫秒
    - 错误类型使用中文描述
  </action>
  <verify>
    <automated>go test -v ./internal/network/... -run TestNetwork</automated>
  </verify>
  <done>
    - internal/network/monitor.go 存在并包含 NetworkMonitor, ConnectivityState 结构体
    - internal/network/monitor_test.go 存在并包含测试用例
    - go test -v ./internal/network/... 通过
    - NetworkMonitor 实现以下方法: NewNetworkMonitor, Start, Stop, GetState
    - checkConnectivity 方法记录 INFO (成功) 或 ERROR (失败) 日志
    - classifyError 方法正确分类: DNS 解析失败、连接超时、连接被拒绝、TLS 证书错误
    - HTTP 客户端禁用重定向跟随 (CheckRedirect 返回 http.ErrUseLastResponse)
  </done>
</task>

</tasks>

<verification>
## Phase-Level Verification

运行完整测试套件:
```bash
go test -v ./...
```

验证 NetworkMonitor 行为:
1. 成功检查: HTTP 200 -> INFO 日志 + "Google 连通性检查成功"
2. 失败检查: HTTP 非 200 或网络错误 -> ERROR 日志 + "Google 连通性检查失败"
3. 状态追踪: 状态变化时记录 "连通性状态改变"
4. 优雅关闭: Stop() 后监控循环退出
</verification>

<success_criteria>
- [x] MONITOR-01: NetworkMonitor 定期向 https://www.google.com 发送 HTTP HEAD 请求
- [x] MONITOR-02: HTTP 非 200 或网络错误时记录 ERROR 日志，包含错误类型分类
- [x] MONITOR-03: HTTP 200 OK 时记录 INFO 日志，包含响应时间
- [x] 状态追踪: ConnectivityState 记录上一次连通性状态
- [x] 错误分类: classifyError 正确分类常见错误类型
- [x] 禁用重定向: CheckRedirect 配置为返回 http.ErrUseLastResponse
</success_criteria>

<output>
After completion, create `.planning/phases/26-network-monitoring-core/26-01-SUMMARY.md`
</output>
