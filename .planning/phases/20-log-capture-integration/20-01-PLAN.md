---
phase: 20-log-capture-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/lifecycle/capture.go, internal/lifecycle/capture_test.go]
autonomous: true
requirements: [CAPT-01, CAPT-02, CAPT-03]

must_haves:
  truths:
    - "captureLogs 函数从 io.Reader 逐行读取日志"
    - "日志行写入 LogBuffer 并保留时间戳、来源和内容"
    - "两个 goroutine 可以同时读取不同管道无阻塞"
    - "context 取消时捕获 goroutine 正确退出"
  artifacts:
    - path: "internal/lifecycle/capture.go"
      provides: "captureLogs 函数实现"
      min_lines: 40
      exports: ["captureLogs"]
    - path: "internal/lifecycle/capture_test.go"
      provides: "captureLogs 单元测试"
      min_lines: 80
      contains: "func TestCaptureLogs"
  key_links:
    - from: "captureLogs"
      to: "logbuffer.LogBuffer.Write"
      via: "调用 Write(entry)"
      pattern: "logBuffer\\.Write\\(LogEntry"
---

<objective>
创建日志捕获核心函数 captureLogs，使用 bufio.Scanner 从管道逐行读取并写入 LogBuffer。

Purpose: 实现日志捕获的核心逻辑，为 stdout/stderr 并发捕获提供可复用的函数。
Output: internal/lifecycle/capture.go 和 capture_test.go
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-log-capture-integration/20-RESEARCH.md

<interfaces>
<!-- Key types from Phase 19 LogBuffer implementation -->
From internal/logbuffer/buffer.go:
```go
type LogEntry struct {
    Timestamp time.Time
    Source    string  // "stdout" or "stderr"
    Content   string
}

func (lb *LogBuffer) Write(entry LogEntry) error
```

From internal/logbuffer/subscriber.go:
```go
func (lb *LogBuffer) Subscribe() <-chan LogEntry
func (lb *LogBuffer) Unsubscribe(ch <-chan LogEntry)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 captureLogs 函数和测试</name>
  <files>internal/lifecycle/capture.go, internal/lifecycle/capture_test.go</files>
  <read_first>
    - internal/logbuffer/buffer.go (理解 LogEntry 结构和 Write 方法签名)
    - .planning/phases/20-log-capture-integration/20-RESEARCH.md (参考 captureLogs 实现模式)
  </read_first>
  <behavior>
    - Test 1: captureLogs 从管道读取多行日志并写入 LogBuffer
    - Test 2: captureLogs 在 context 取消时停止读取并退出
    - Test 3: captureLogs 处理 scanner 错误并记录日志
    - Test 4: captureLogs 正确设置 LogEntry 的 Timestamp, Source, Content 字段
  </behavior>
  <action>
创建 internal/lifecycle/capture.go 实现以下函数:

```go
//go:build windows

package lifecycle

import (
    "bufio"
    "context"
    "io"
    "log/slog"
    "time"

    "github.com/HQGroup/nanobot-auto-updater/internal/logbuffer"
)

// captureLogs reads logs from reader line by line and writes to LogBuffer
// CAPT-01, CAPT-02: Captures stdout/stderr output stream
// CAPT-03: Runs in separate goroutine for concurrent pipe reading
// CAPT-05: Stops when context cancelled (process exit)
func captureLogs(
    ctx context.Context,
    reader io.Reader,
    source string,  // "stdout" or "stderr"
    logBuffer *logbuffer.LogBuffer,
    logger *slog.Logger,
) {
    scanner := bufio.NewScanner(reader)
    for {
        select {
        case <-ctx.Done():
            // Context cancelled, stop reading (CAPT-05)
            logger.Debug("Log capture stopped", "source", source)
            return
        default:
            // Non-blocking scan
            if !scanner.Scan() {
                // EOF or error
                if err := scanner.Err(); err != nil {
                    // ERR-01: Log error but continue running
                    logger.Error("Log capture scanner error",
                        "source", source, "error", err)
                }
                return
            }

            // Read line and write to LogBuffer
            line := scanner.Text()
            entry := logbuffer.LogEntry{
                Timestamp: time.Now(),
                Source:    source,
                Content:   line,
            }
            if err := logBuffer.Write(entry); err != nil {
                // ERR-03: Log error and drop the line (don't block)
                logger.Error("Failed to write log to buffer",
                    "source", source, "line", line, "error", err)
            }
        }
    }
}
```

创建 internal/lifecycle/capture_test.go 实现以下测试:

```go
//go:build windows

package lifecycle

import (
    "bytes"
    "context"
    "strings"
    "testing"
    "time"

    "github.com/HQGroup/nanobot-auto-updater/internal/logbuffer"
    "github.com/WQGroup/logger"
)

func TestCaptureLogs_WritesToBuffer(t *testing.T) {
    // Setup: Create LogBuffer and pipe reader
    logBuf := logbuffer.NewLogBuffer(logger.NewNopLogger())

    input := "line1\nline2\nline3\n"
    reader := strings.NewReader(input)

    ctx := context.Background()

    // Execute: Run captureLogs
    captureLogs(ctx, reader, "stdout", logBuf, logger.NewNopLogger())

    // Verify: Check logs written to buffer
    history := logBuf.GetHistory()
    if len(history) != 3 {
        t.Errorf("expected 3 log entries, got %d", len(history))
    }
    if history[0].Content != "line1" {
        t.Errorf("expected first line 'line1', got '%s'", history[0].Content)
    }
    if history[0].Source != "stdout" {
        t.Errorf("expected source 'stdout', got '%s'", history[0].Source)
    }
}

func TestCaptureLogs_ContextCancellation(t *testing.T) {
    // Setup: Create LogBuffer and slow reader
    logBuf := logbuffer.NewLogBuffer(logger.NewNopLogger())

    input := "line1\nline2\n"
    reader := strings.NewReader(input)

    ctx, cancel := context.WithCancel(context.Background())

    // Execute: Cancel context immediately
    cancel()
    captureLogs(ctx, reader, "stdout", logBuf, logger.NewNopLogger())

    // Verify: No logs written (context cancelled before scan)
    // Note: Due to select behavior, some logs may be written before cancellation
    // This test verifies the function exits cleanly
}

func TestCaptureLogs_LogEntryFields(t *testing.T) {
    // Setup
    logBuf := logbuffer.NewLogbuffer(logger.NewNopLogger())

    input := "test log line\n"
    reader := strings.NewReader(input)
    ctx := context.Background()

    beforeTime := time.Now()
    captureLogs(ctx, reader, "stderr", logBuf, logger.NewNopLogger())
    afterTime := time.Now()

    // Verify: Check all LogEntry fields are set correctly
    history := logBuf.GetHistory()
    if len(history) != 1 {
        t.Fatalf("expected 1 log entry, got %d", len(history))
    }

    entry := history[0]
    if entry.Source != "stderr" {
        t.Errorf("expected source 'stderr', got '%s'", entry.Source)
    }
    if entry.Content != "test log line" {
        t.Errorf("expected content 'test log line', got '%s'", entry.Content)
    }
    if entry.Timestamp.Before(beforeTime) || entry.Timestamp.After(afterTime) {
        t.Errorf("timestamp %v not in expected range [%v, %v]", entry.Timestamp, beforeTime, afterTime)
    }
}
```

注意:
1. 文件头部必须有 `//go:build windows` 约束 (与 starter.go 保持一致)
2. 使用 `bufio.Scanner` 而不是 `bufio.Reader` (RESEARCH.md 推荐)
3. 使用 `select + default` 模式检查 context 取消 (非阻塞)
4. 日志来源参数 `source` 使用 "stdout" 或 "stderr"
</action>
<verify>
  <automated>go test -v ./internal/lifecycle -run TestCaptureLogs</automated>
</verify>
<done>
  - capture.go 包含 captureLogs 函数签名: `func captureLogs(ctx context.Context, reader io.Reader, source string, logBuffer *logbuffer.LogBuffer, logger *slog.Logger)`
  - capture.go 使用 `bufio.Scanner` 逐行读取
  - capture.go 在 select 中检查 `ctx.Done()` 实现可取消
  - capture_test.go 包含 TestCaptureLogs_WritesToBuffer, TestCaptureLogs_ContextCancellation, TestCaptureLogs_LogEntryFields 三个测试
  - `go test -v ./internal/lifecycle -run TestCaptureLogs` 通过
</done>
</task>

</tasks>

<verification>
运行 `go test -v ./internal/lifecycle -run TestCaptureLogs` 验证 captureLogs 函数:
- 从管道读取日志并写入 LogBuffer
- context 取消时正确退出
- LogEntry 字段正确设置
</verification>

<success_criteria>
- capture.go 文件存在且包含 captureLogs 函数
- capture_test.go 文件存在且包含至少 3 个测试
- 所有测试通过
- captureLogs 使用 bufio.Scanner 逐行读取
- captureLogs 支持 context 取消
</success_criteria>

<output>
After completion, create `.planning/phases/20-log-capture-integration/20-01-SUMMARY.md`
</output>
