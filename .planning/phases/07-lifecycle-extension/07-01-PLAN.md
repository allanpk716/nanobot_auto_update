---
phase: 07-lifecycle-extension
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/instance/errors.go
  - internal/instance/errors_test.go
  - internal/instance/lifecycle.go
  - internal/instance/lifecycle_test.go
  - internal/lifecycle/starter.go
autonomous: true
requirements:
  - LIFECYCLE-01
  - LIFECYCLE-02
must_haves:
  truths:
    - "每个实例的所有日志消息都包含实例名称"
    - "系统可以为特定名称的实例执行停止操作"
    - "系统可以为特定名称的实例执行启动操作"
    - "停止和启动操作复用现有的 v1.0 生命周期逻辑"
  artifacts:
    - path: "internal/instance/errors.go"
      provides: "InstanceError 自定义错误类型"
      exports: ["InstanceError", "Error", "Unwrap"]
    - path: "internal/instance/lifecycle.go"
      provides: "InstanceLifecycle 包装器"
      exports: ["NewInstanceLifecycle", "StopForUpdate", "StartAfterUpdate"]
    - path: "internal/lifecycle/starter.go"
      provides: "重构后的 StartNanobot 函数"
      contains: "func StartNanobot(ctx context.Context, command string, port uint32"
  key_links:
    - from: "internal/instance/lifecycle.go"
      to: "internal/lifecycle/detector.go"
      via: "lifecycle.IsNanobotRunning(il.config.Port)"
      pattern: "lifecycle\\.IsNanobotRunning"
    - from: "internal/instance/lifecycle.go"
      to: "internal/lifecycle/stopper.go"
      via: "lifecycle.StopNanobot(ctx, pid, timeout, logger)"
      pattern: "lifecycle\\.StopNanobot"
    - from: "internal/instance/lifecycle.go"
      to: "internal/lifecycle/starter.go"
      via: "lifecycle.StartNanobot(ctx, command, port, timeout, logger)"
      pattern: "lifecycle\\.StartNanobot.*command.*port"
    - from: "internal/instance/lifecycle.go"
      to: "internal/config/instance.go"
      via: "config.InstanceConfig"
      pattern: "config\\.InstanceConfig"
---

<objective>
实现实例生命周期包装器和自定义错误类型,为每个实例提供独立的上下文感知生命周期管理,支持定制化启动命令和结构化错误报告

Purpose: 为多实例场景提供可追踪、可调试的生命周期管理能力,每个实例的日志包含实例名称,错误包含结构化信息
Output: InstanceLifecycle 包装器,InstanceError 自定义错误类型,重构后的 StartNanobot 函数
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-lifecycle-extension/07-CONTEXT.md
@.planning/phases/07-lifecycle-extension/07-RESEARCH.md
@.planning/phases/06-configuration-extension/06-01-SUMMARY.md
@.planning/phases/06-configuration-extension/06-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From internal/config/instance.go:
```go
type InstanceConfig struct {
    Name           string        `mapstructure:"name"`
    Port           uint32        `mapstructure:"port"`
    StartCommand   string        `mapstructure:"start_command"`
    StartupTimeout time.Duration `mapstructure:"startup_timeout"`
}

func (ic *InstanceConfig) Validate() error
```

From internal/lifecycle/manager.go:
```go
type Manager struct {
    port           uint32
    startupTimeout time.Duration
    stopTimeout    time.Duration
    logger         *slog.Logger
}

func (m *Manager) StopForUpdate(ctx context.Context) error
func (m *Manager) StartAfterUpdate(ctx context.Context) error
```

From internal/lifecycle/detector.go:
```go
func IsNanobotRunning(port uint32) (running bool, pid int32, detectionMethod string, err error)
```

From internal/lifecycle/stopper.go:
```go
func StopNanobot(ctx context.Context, pid int32, timeout time.Duration, logger *slog.Logger) error
```

From internal/lifecycle/starter.go (will be refactored):
```go
func StartNanobot(ctx context.Context, startupTimeout time.Duration, logger *slog.Logger) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: 实现 InstanceError 自定义错误类型</name>
<files>internal/instance/errors.go, internal/instance/errors_test.go</files>
<behavior>
- Test 1: InstanceError.Error() 返回格式化的中文错误消息,包含实例名称、操作类型、端口和底层错误
- Test 2: InstanceError.Unwrap() 返回底层错误,支持 errors.Is/As 遍历错误链
- Test 3: InstanceError 支持不同的操作类型(stop/start),生成对应的中文操作文本
- Test 4: nil 底层错误时 Error() 不崩溃
</behavior>
<action>
使用 TDD RED-GREEN-REFACTOR 模式实现 InstanceError 自定义错误类型:

**RED Phase (创建测试):**
1. 创建 internal/instance/errors_test.go
2. 编写 TestInstanceError_Error 测试错误消息格式:
   - 停止操作: "停止实例 "test-instance" 失败 (port=18790): process not found"
   - 启动操作: "启动实例 "test-instance" 失败 (port=18790): timeout waiting for port"
3. 编写 TestInstanceError_Unwrap 测试错误链遍历:
   - 使用 errors.Is() 验证可以匹配底层错误
   - 使用 errors.As() 验证可以提取 InstanceError
4. 编写 TestInstanceError_operationText 测试操作类型文本转换
5. 运行 `go test ./internal/instance -v -run TestInstanceError` 确认失败

**GREEN Phase (实现功能):**
1. 创建 internal/instance/errors.go
2. 定义 InstanceError 结构体:
   ```go
   type InstanceError struct {
       InstanceName string
       Operation    string // "stop" or "start"
       Port         uint32
       Err          error
   }
   ```
3. 实现 Error() 方法,返回格式化的中文错误消息
4. 实现 Unwrap() 方法返回底层错误
5. 实现 operationText() 辅助方法转换操作类型为中文
6. 运行 `go test ./internal/instance -v -run TestInstanceError` 确认通过

**REFACTOR Phase (优化):**
- 确保错误消息格式一致
- 添加边界情况测试(nil Err,空 InstanceName 等)

**约束:**
- 使用中文错误消息("停止实例"/"启动实例")
- 必须实现 Unwrap() 方法支持错误链
- 操作类型只能是 "stop" 或 "start"
- 不要在 Error() 方法中修改底层错误消息
</action>
<verify>
<automated>go test ./internal/instance -v -run TestInstanceError</automated>
</verify>
<done>
- InstanceError 结构体定义完成
- Error() 方法返回格式化的中文错误消息
- Unwrap() 方法返回底层错误
- 所有测试通过
</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: 重构 lifecycle.StartNanobot 支持动态命令参数</name>
<files>internal/lifecycle/starter.go</files>
<behavior>
- Test 1: StartNanobot 接收 command 参数,使用 cmd /c 执行 Shell 命令
- Test 2: command 参数支持管道、重定向等复杂命令(例如 "nanobot gateway | tee log.txt")
- Test 3: 端口验证使用传入的 port 参数而非固定端口
- Test 4: 现有调用 StartNanobot(ctx, timeout, logger) 的代码需要更新为 StartNanobot(ctx, "nanobot gateway", 18790, timeout, logger)
</behavior>
<action>
重构现有 lifecycle.StartNanobot() 函数,使其接收动态命令参数和端口参数:

**修改签名:**
```go
// OLD: func StartNanobot(ctx context.Context, startupTimeout time.Duration, logger *slog.Logger) error
// NEW: func StartNanobot(ctx context.Context, command string, port uint32, startupTimeout time.Duration, logger *slog.Logger) error
```

**实现变更:**
1. 修改函数签名,添加 `command string` 和 `port uint32` 参数
2. 替换 `exec.Command("nanobot", "gateway")` 为 `exec.CommandContext(ctx, "cmd", "/c", command)`
3. 替换 `waitForProcessRunning(ctx, "nanobot.exe", startupTimeout, logger)` 为 `waitForPortListening(ctx, port, startupTimeout, logger)`
4. 更新日志消息包含 command 和 port 参数
5. 保留现有的 SysProcAttr 配置(HideWindow, CREATE_NO_WINDOW, CREATE_NEW_PROCESS_GROUP)
6. 保留现有的 Process.Release() 调用确保进程独立运行

**验证逻辑变更:**
- 移除 waitForProcessRunning 调用(基于进程名验证不够精确)
- 改用 waitForPortListening 验证端口监听(已有实现,可直接复用)

**调用点更新:**
- internal/lifecycle/manager.go:68 调用需要更新
  - OLD: `StartNanobot(ctx, m.startupTimeout, m.logger)`
  - NEW: `StartNanobot(ctx, "nanobot gateway", m.port, m.startupTimeout, m.logger)`

**注意:**
- 不需要创建新的测试文件,因为 starter.go 原有测试不存在(通过集成测试验证)
- manager.go 的更新会在 Task 3 中测试
- 确保符合 Windows Shell 执行方式的最佳实践(cmd /c 支持管道和重定向)
</action>
<verify>
<automated>go test ./internal/lifecycle -v</automated>
</verify>
<done>
- StartNanobot 函数签名更新为接收 command 和 port 参数
- 使用 cmd /c 执行 Shell 命令
- 使用端口验证而非进程名验证
- manager.go 调用点更新完成
</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: 实现 InstanceLifecycle 包装器</name>
<files>internal/instance/lifecycle.go, internal/instance/lifecycle_test.go</files>
<behavior>
- Test 1: NewInstanceLifecycle 构造时注入 logger 包含 instance 和 component 字段
- Test 2: StopForUpdate 调用 lifecycle.IsNanobotRunning 和 lifecycle.StopNanobot,返回 InstanceError
- Test 3: StartAfterUpdate 调用 lifecycle.StartNanobot 使用实例的 command 和 port,返回 InstanceError
- Test 4: 日志输出包含 instance 和 component 字段
- Test 5: 实例未运行时 StopForUpdate 返回 nil(不是错误)
- Test 6: StartupTimeout 为 0 时使用 30s 默认值
</behavior>
<action>
使用 TDD RED-GREEN-REFACTOR 模式实现 InstanceLifecycle 包装器:

**RED Phase (创建测试):**
1. 创建 internal/instance/lifecycle_test.go
2. 编写 TestNewInstanceLifecycle_LoggerContextInjection 测试日志注入:
   - 验证 logger.With("instance", name).With("component", "instance-lifecycle")
   - 使用 mock logger 捕获输出验证字段存在
3. 编写 TestInstanceLifecycle_StopForUpdate 测试停止逻辑:
   - Mock lifecycle.IsNanobotRunning 返回 running=true
   - 验证调用 lifecycle.StopNanobot
   - 验证返回的 InstanceError 包含正确的实例信息
4. 编写 TestInstanceLifecycle_StartAfterUpdate 测试启动逻辑:
   - 验证调用 lifecycle.StartNanobot 使用实例的 command 和 port
   - 验证返回的 InstanceError 包含正确的实例信息
   - 验证 StartupTimeout=0 时使用 30s 默认值
5. 编写 TestInstanceLifecycle_StopForUpdate_NotRunning 测试实例未运行场景
6. 运行 `go test ./internal/instance -v -run TestInstanceLifecycle` 确认失败

**GREEN Phase (实现功能):**
1. 创建 internal/instance/lifecycle.go
2. 定义 InstanceLifecycle 结构体:
   ```go
   type InstanceLifecycle struct {
       config config.InstanceConfig
       logger *slog.Logger
   }
   ```
3. 实现 NewInstanceLifecycle 构造函数:
   - 调用 `logger.With("instance", cfg.Name).With("component", "instance-lifecycle")` 创建实例专属 logger
   - 返回 &InstanceLifecycle{config: cfg, logger: instanceLogger}
4. 实现 StopForUpdate(ctx context.Context) error:
   - 调用 `lifecycle.IsNanobotRunning(il.config.Port)` 检测实例运行状态
   - 如果未运行,记录日志并返回 nil
   - 如果运行中,调用 `lifecycle.StopNanobot(ctx, pid, 5*time.Second, il.logger)` 停止实例
   - 失败时返回 &InstanceError{InstanceName, "stop", Port, err}
5. 实现 StartAfterUpdate(ctx context.Context) error:
   - 处理 StartupTimeout=0 的情况,使用 30*time.Second 默认值
   - 调用 `lifecycle.StartNanobot(ctx, il.config.StartCommand, il.config.Port, startupTimeout, il.logger)` 启动实例
   - 失败时返回 &InstanceError{InstanceName, "start", Port, err}
6. 运行 `go test ./internal/instance -v -run TestInstanceLifecycle` 确认通过

**REFACTOR Phase (优化):**
- 确保所有日志调用使用预注入的 logger(不需要重复添加 instance 字段)
- 统一错误处理模式(使用 InstanceError 包装所有底层错误)
- 优化日志消息的可读性

**Mock 策略:**
- 由于无法在单元测试中启动真实进程,使用以下策略:
  - 创建 internal/instance/mocks.go 提供 mock 函数(可选,如果测试需要隔离)
  - 或者使用集成测试验证真实进程管理(推荐)

**约束:**
- 必须调用 lifecycle 包的函数,不重新实现底层逻辑
- 停止超时固定为 5 秒(与 v1.0 一致)
- 启动超时默认 30 秒(如果配置为 0)
- 所有日志必须包含 instance 和 component 字段
</action>
<verify>
<automated>go test ./internal/instance -v -run TestInstanceLifecycle</automated>
</verify>
<done>
- InstanceLifecycle 结构体定义完成
- NewInstanceLifecycle 构造时注入 logger 上下文
- StopForUpdate 方法实现完成,调用 lifecycle 包函数
- StartAfterUpdate 方法实现完成,使用实例配置的 command 和 port
- 所有日志包含 instance 和 component 字段
- 所有测试通过
</done>
</task>

</tasks>

<verification>
Phase 7 验证通过条件:
1. InstanceError 单元测试全部通过(Error, Unwrap, operationText)
2. InstanceLifecycle 单元测试全部通过(日志注入, StopForUpdate, StartAfterUpdate)
3. lifecycle.StartNanobot 重构完成,支持动态命令和端口参数
4. 代码覆盖率 ≥ 80% (internal/instance 包)
5. 所有日志输出包含 instance 和 component 字段(手动验证)
6. InstanceError 错误消息格式正确,支持错误链遍历(手动验证)
</verification>

<success_criteria>
Phase 7 阶段成功标准:
1. ✅ 每个实例的所有日志消息都包含实例名称(logger.With 预注入)
2. ✅ 系统可以为特定名称的实例执行停止操作(StopForUpdate 方法)
3. ✅ 系统可以为特定名称的实例执行启动操作(StartAfterUpdate 方法)
4. ✅ 停止和启动操作复用现有的 v1.0 生命周期逻辑(调用 lifecycle 包函数)
5. ✅ InstanceError 提供结构化错误信息,支持类型断言和错误链遍历
6. ✅ StartNanobot 支持动态命令参数,使用 Shell 执行方式
</success_criteria>

<output>
After completion, create `.planning/phases/07-lifecycle-extension/07-01-SUMMARY.md`
</output>
